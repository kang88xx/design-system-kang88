/* Generated by scripts/build-samples.py. Edit kit files and sample JSON instead. */
window.ReferenceLabData = {"samples": [{"id": "hero", "title": "히어로 포커스 펄스", "summary": "가느다란 축과 좌우 대칭 원을 유지하면서 중앙 원이 확장됐다가 돌아옵니다. 다시 재생과 속도로 움직임을 비교하세요.", "sourceTime": "00:00-00:05", "evidence": "00:00–00:05 중앙 검은 원의 확장·축소를 확인했습니다. 선 그리기와 반복 주기 4.8초는 재구성한 제안값입니다.", "status": "video-observed", "html": "<section class=\"rm-sample\" data-rm=\"hero\" aria-label=\"히어로 포커스 펄스\"><div class=\"rm-sample-head\"><div><h3>히어로 포커스 펄스</h3><p>관찰한 도형과 중앙 확장 효과. 시간과 easing은 재구성입니다.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-stage rm-hero\" aria-hidden=\"true\"><svg class=\"rm-hero-grid\" viewBox=\"0 0 1000 410\" aria-hidden=\"true\"><path class=\"rm-hero-axis\" d=\"M60 205H940M500 35V375\"/><g><circle class=\"rm-hero-ring\" cx=\"270\" cy=\"205\" r=\"112\"/><circle class=\"rm-hero-ring\" cx=\"360\" cy=\"205\" r=\"86\"/><circle class=\"rm-hero-ring\" cx=\"452\" cy=\"205\" r=\"43\"/><circle class=\"rm-hero-ring\" cx=\"548\" cy=\"205\" r=\"43\"/><circle class=\"rm-hero-ring\" cx=\"640\" cy=\"205\" r=\"86\"/><circle class=\"rm-hero-ring\" cx=\"730\" cy=\"205\" r=\"112\"/></g><circle class=\"rm-hero-core rm-loop\" cx=\"500\" cy=\"205\" r=\"14\"/><g class=\"rm-hero-label\"><rect x=\"82\" y=\"52\" width=\"210\" height=\"32\"/><text x=\"96\" y=\"73\">Ideas become connections.</text><rect x=\"702\" y=\"326\" width=\"220\" height=\"32\"/><text x=\"716\" y=\"347\">Connections create impact.</text></g></svg></div></section>", "kind": "motion"}, {"id": "benefits", "title": "6가지 기하 아이콘", "summary": "선 그리기·원 확장·점 이동을 작은 카드에 적용합니다. hover·키보드 포커스·터치로 상태를 바꿔 보세요.", "sourceTime": "00:05-00:09 and 00:46-00:48", "evidence": "00:05–00:09에 작은 기하 도형 6종이 보입니다. hover·focus·touch에 따른 반응은 실제 적용을 위한 제안입니다.", "status": "recommended", "html": "<section class=\"rm-sample\" data-rm=\"benefits\" aria-label=\"6가지 기하 아이콘\"><div class=\"rm-sample-head\"><div><h3>6가지 기하 아이콘</h3><p>hover·포커스·터치로 도형 반응을 확인하세요.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-benefits-grid\"><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Reach diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><circle data-rm-part=\"1\" cx=\"64\" cy=\"62\" r=\"32\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"124\" cy=\"62\" r=\"20\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"164\" cy=\"62\" r=\"9\" fill=\"currentColor\" opacity=\".28\"/></svg></button><h4>Reach</h4><p>Expanding circles map audience growth.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Targeting diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><rect data-rm-part=\"1\" x=\"38\" y=\"28\" width=\"58\" height=\"68\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"126\" cy=\"62\" r=\"30\" fill=\"none\" stroke=\"currentColor\"/><path data-rm-part=\"3\" d=\"M150 36l34 26-34 26z\" fill=\"currentColor\"/></svg></button><h4>Targeting</h4><p>Simple shapes show narrowed context.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Creative diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><path data-rm-part=\"1\" d=\"M46 88L88 36l42 52z\" fill=\"none\" stroke=\"currentColor\"/><rect data-rm-part=\"2\" x=\"120\" y=\"32\" width=\"54\" height=\"54\" fill=\"currentColor\" opacity=\".18\"/><circle data-rm-part=\"3\" cx=\"148\" cy=\"62\" r=\"18\" fill=\"currentColor\"/></svg></button><h4>Creative</h4><p>Composed pieces preview ad assembly.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Conversation diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><circle data-rm-part=\"1\" cx=\"76\" cy=\"54\" r=\"28\" fill=\"none\" stroke=\"currentColor\"/><path data-rm-part=\"2\" d=\"M104 48h70v36h-70z\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"126\" cy=\"66\" r=\"5\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"146\" cy=\"66\" r=\"5\" fill=\"currentColor\"/></svg></button><h4>Conversation</h4><p>Small responses imply active posts.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Measurement diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><rect data-rm-part=\"1\" x=\"48\" y=\"72\" width=\"28\" height=\"24\" fill=\"currentColor\" opacity=\".2\"/><rect data-rm-part=\"2\" x=\"96\" y=\"48\" width=\"28\" height=\"48\" fill=\"currentColor\" opacity=\".45\"/><rect data-rm-part=\"3\" x=\"144\" y=\"28\" width=\"28\" height=\"68\" fill=\"currentColor\"/><path d=\"M42 96h138\" stroke=\"currentColor\"/></svg></button><h4>Measure</h4><p>Bars step upward from observed metrics.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Launch diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><ellipse data-rm-part=\"1\" cx=\"110\" cy=\"64\" rx=\"72\" ry=\"28\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"110\" cy=\"64\" r=\"18\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"178\" cy=\"64\" r=\"8\" fill=\"currentColor\" opacity=\".28\"/></svg></button><h4>Launch</h4><p>An orbit motif connects to the CTA section.</p></article></div></section>", "kind": "motion"}, {"id": "orbit", "title": "크레딧 궤도 효과", "summary": "점선 궤도와 움직이는 위성이 중앙 CTA를 둘러쌉니다. 화면 밖에서는 반복 동작을 멈춥니다.", "sourceTime": "00:08-00:11 and 00:46-00:47", "evidence": "00:08–00:11의 크레딧 영역과 점선·실선 궤도를 바탕으로 구성했습니다. 위성 이동·선 그리기의 타이밍은 재구성입니다.", "status": "video-observed", "html": "<section class=\"rm-sample\" data-rm=\"orbit\" aria-label=\"크레딧 궤도 효과\"><div class=\"rm-sample-head\"><div><h3>크레딧 궤도 효과</h3><p>화면 밖·비활성 탭·일시정지 상태에서는 반복을 멈춥니다.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-stage rm-orbit\"><div class=\"rm-orbit-wrap\"><svg viewBox=\"0 0 620 376\" aria-hidden=\"true\"><ellipse class=\"rm-orbit-ring\" cx=\"310\" cy=\"188\" rx=\"256\" ry=\"118\"/><ellipse class=\"rm-orbit-ring\" cx=\"310\" cy=\"188\" rx=\"196\" ry=\"76\"/><line x1=\"54\" y1=\"188\" x2=\"566\" y2=\"188\" stroke=\"currentColor\" opacity=\".18\"/></svg><span class=\"rm-satellite rm-loop\" aria-hidden=\"true\"></span><div class=\"rm-credit-card\"><span>Ad credit</span><strong>$500</strong><button class=\"rm-button\" type=\"button\">Start campaign</button></div></div></div></section>", "kind": "motion"}, {"id": "metrics", "title": "계단형 지표 · 카운트업", "summary": "높이가 다른 카드가 순서대로 나타나고 숫자가 목표값에 도달합니다. 수치는 예제 데이터입니다.", "sourceTime": "00:11-00:13", "evidence": "00:11–00:13에 계단형 지표 카드가 보입니다. 숫자 카운트업과 등장 시차는 영상에서 확정되지 않은 적용 제안입니다.", "status": "recommended", "html": "<section class=\"rm-sample\" data-rm=\"metrics\" aria-label=\"계단형 지표 · 카운트업\"><div class=\"rm-sample-head\"><div><h3>계단형 지표 · 카운트업</h3><p>Entrance and numbers are controlled by the kit API.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-metrics-grid\"><article class=\"rm-metric\" style=\"--rm-height:150px\"><strong data-rm-count=\"42\" data-rm-suffix=\"%\">0</strong><p>More qualified reach</p></article><article class=\"rm-metric\" style=\"--rm-height:198px\"><strong data-rm-count=\"3.8\" data-rm-suffix=\"x\">0</strong><p>Campaign learning speed</p></article><article class=\"rm-metric\" style=\"--rm-height:246px\"><strong data-rm-count=\"1280\" data-rm-prefix=\"+\">0</strong><p>Measured actions</p></article></div></section>", "kind": "motion"}, {"id": "reveal", "title": "스크롤 순차 등장", "summary": "뷰포트 진입을 감지해 편집형 카드를 순서대로 보여줍니다. 모션 감소 환경에서는 콘텐츠를 즉시 표시합니다.", "sourceTime": "00:00-00:49 scrolling behavior", "evidence": "영상에서 긴 페이지의 스크롤은 확인되지만 fade-up은 확정할 수 없습니다. 이 샘플은 선택적으로 적용하는 효과입니다.", "status": "recommended", "html": "<section class=\"rm-sample\" data-rm=\"reveal\" aria-label=\"스크롤 순차 등장\"><div class=\"rm-sample-head\"><div><h3>스크롤 순차 등장</h3><p>Scroll-driven entrance for long-form sections.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-reveal-list\"><article class=\"rm-editorial\"><span class=\"rm-index\">01</span><div><h4>Set one clear outcome</h4><p>Keep the first message and first action readable before details appear.</p></div></article><article class=\"rm-editorial\"><span class=\"rm-index\">02</span><div><h4>Show the mechanism</h4><p>Use compact diagrams to explain how targeting, formats, and feedback connect.</p></div></article><article class=\"rm-editorial\"><span class=\"rm-index\">03</span><div><h4>Measure and refine</h4><p>Bring metrics forward with a predictable, restrained motion rhythm.</p></div></article></div></section>", "kind": "motion"}, {"id": "formats", "title": "6가지 광고 프리뷰 효과", "summary": "이미지 공개, 재생 진행률, 슬라이드, 카드 반응, 상품 변경, 숫자 피드백을 코드로 재생합니다.", "sourceTime": "00:19-00:27 and 00:39-00:43", "evidence": "00:19–00:27의 광고 형식 그리드를 바탕으로 구성했습니다. 각 프리뷰 루프·hover·카운트는 원본 동작이 아닌 적용 예시입니다.", "status": "recommended", "html": "<section class=\"rm-sample\" data-rm=\"formats\" aria-label=\"6가지 광고 프리뷰 효과\"><div class=\"rm-sample-head\"><div><h3>6가지 광고 프리뷰 효과</h3><p>Six clear demo states with loops, hover, and numeric updates.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-formats-grid\"><article class=\"rm-format\"><div class=\"rm-format-demo rm-image-reveal\"><span class=\"rm-photo\"></span><span class=\"rm-mask\"></span></div><h4>Image reveal</h4><p>Mask pass exposes creative.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-video-progress\"><span class=\"rm-video-box\"></span><span class=\"rm-play\"></span><span class=\"rm-progress\"></span></div><h4>Video progress</h4><p>Progress bar loops while visible.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-carousel\"><span class=\"rm-slide\">01</span></div><h4>Carousel slide</h4><p>Sequential panels rotate with WAAPI fallback.</p></article><article class=\"rm-format\"><button type=\"button\" class=\"rm-format-demo rm-promoted\" aria-label=\"Promoted post lift\"><span class=\"rm-post\"><span class=\"rm-line-a\"></span><span class=\"rm-line-b\"></span><span class=\"rm-line-c\"></span></span></button><h4>Promoted post</h4><p>Post lifts on hover, focus, or touch.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-product-cycle\"><span class=\"rm-product\"></span><span class=\"rm-price\"><span>Product</span><strong>$48</strong></span></div><h4>Product cycle</h4><p>Shape changes suggest catalog rotation.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-boost-counts\"><span class=\"rm-ping\"></span><span class=\"rm-count\">+1,240</span></div><h4>Boost counts</h4><p>Live-style count increments while active.</p></article></div></section>", "kind": "motion"}, {"id": "buttons", "title": "버튼 · 상태 피드백", "summary": "hover·pressed·focus·disabled와 요청 중·완료 상태를 직접 확인하세요. 원격 요청 없이 동작합니다.", "sourceTime": "00:00–00:04", "evidence": "pill CTA를 바탕으로 사용 상태를 보완했습니다. hover·로딩·완료 피드백은 영상에서 확인되지 않은 적용 제안입니다.", "status": "recommended", "html": "<section class=\"ri-sample\" data-ri=\"buttons\" aria-labelledby=\"ri-buttons-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-buttons-title\">버튼 상태</h3><p class=\"ri-copy\">기본 CTA는 44px 이상 터치 영역, 포커스 링, hover 화살표 이동, 눌림 상태, 비활성 상태, 로컬 loading-to-confirm 피드백을 포함합니다.</p><div class=\"ri-button-row\" style=\"margin-top:24px\"><button class=\"ri-button ri-button-primary\" type=\"button\" data-ri-button-action aria-busy=\"false\">확인 요청 <span class=\"ri-button-arrow\" aria-hidden=\"true\">-&gt;</span></button><button class=\"ri-button\" type=\"button\">보조 행동 <span class=\"ri-button-arrow\" aria-hidden=\"true\">-&gt;</span></button><button class=\"ri-button\" type=\"button\" aria-pressed=\"true\">선택됨</button><button class=\"ri-button\" type=\"button\" disabled>비활성</button></div><p class=\"ri-status\" data-ri-button-status role=\"status\" aria-live=\"polite\">버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.</p></div></section>", "kind": "interaction"}, {"id": "tabs", "title": "캠페인 4단계 탭", "summary": "선택한 단계에 따라 패널과 밑줄이 전환됩니다. 방향키·Home·End로도 탐색할 수 있습니다.", "sourceTime": "00:14-00:18", "evidence": "00:14–00:18에 선택 단계와 패널 변화가 보입니다. 이동 밑줄과 240ms 전환은 제안값이며 파랑은 패널 내부 콘텐츠에만 사용합니다.", "status": "video-observed", "html": "<section class=\"ri-sample\" data-ri=\"tabs\" aria-labelledby=\"ri-tabs-title\"><div class=\"ri-shell ri-tabs-layout\"><div><h3 class=\"ri-heading\" id=\"ri-tabs-title\">캠페인 단계 탭</h3><p class=\"ri-copy\">탭 전환은 선택 상태, 얇은 underline 위치, 패널 카피와 mock panel을 함께 갱신합니다. 키보드는 좌우 방향키, Home, End를 지원합니다.</p></div><div class=\"ri-tabs-list\" role=\"tablist\" aria-label=\"캠페인 단계\"><button class=\"ri-tab\" id=\"ri-tab-1\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"true\" data-kicker=\"01 / Objective\" data-title=\"목표를 먼저 정하세요.\" data-copy=\"한 가지 캠페인 목표를 정하면 메시지, 예산, 측정 기준이 같은 방향으로 정렬됩니다.\"><span class=\"ri-tab-index\">01</span><span class=\"ri-tab-label\">목표</span></button><button class=\"ri-tab\" id=\"ri-tab-2\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"02 / Audience\" data-title=\"도달할 사람을 좁히세요.\" data-copy=\"관심사와 맥락을 기준으로 타깃을 정의하면 더 적은 노출로도 명확한 반응을 얻을 수 있습니다.\"><span class=\"ri-tab-index\">02</span><span class=\"ri-tab-label\">대상</span></button><button class=\"ri-tab\" id=\"ri-tab-3\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"03 / Creative\" data-title=\"짧은 소재를 만드세요.\" data-copy=\"큰 메시지 하나, 읽기 쉬운 보조 문장, 분명한 행동 버튼으로 광고 소재를 구성합니다.\"><span class=\"ri-tab-index\">03</span><span class=\"ri-tab-label\">소재</span></button><button class=\"ri-tab\" id=\"ri-tab-4\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"04 / Learn\" data-title=\"결과에서 다음 캠페인을 배우세요.\" data-copy=\"성과를 확인하고 다음 실험으로 이어지는 개선 포인트를 같은 흐름 안에서 기록합니다.\"><span class=\"ri-tab-index\">04</span><span class=\"ri-tab-label\">학습</span></button><span class=\"ri-tabs-indicator\" data-ri-tabs-indicator aria-hidden=\"true\"></span></div><div class=\"ri-tab-panel\" id=\"ri-tab-panel\" role=\"tabpanel\" aria-labelledby=\"ri-tab-1\"><div><p class=\"ri-panel-kicker\" data-ri-panel-kicker>01 / Objective</p><h4 class=\"ri-panel-title\" data-ri-panel-title>목표를 먼저 정하세요.</h4><p class=\"ri-panel-copy\" data-ri-panel-copy>한 가지 캠페인 목표를 정하면 메시지, 예산, 측정 기준이 같은 방향으로 정렬됩니다.</p></div><div class=\"ri-mock-panel\" aria-hidden=\"true\"><div class=\"ri-mock-header\"></div><div class=\"ri-mock-line\"></div><div class=\"ri-mock-line\"></div><div class=\"ri-mock-bars\"><span></span><span></span><span></span><span></span><span></span></div><div class=\"ri-mock-grid\"><div class=\"ri-mock-card\"></div><div class=\"ri-mock-card\"></div></div></div></div></div></section>", "kind": "interaction"}, {"id": "carousel", "title": "인용 카드 캐러셀", "summary": "이전·다음·페이지 점·스와이프로 카드를 탐색합니다. 자동 재생은 직접 켜고 포커스·hover 시 멈춥니다.", "sourceTime": "00:11-00:13 panel observed; controls proposed", "evidence": "00:11–00:13에 어두운 인용 카드가 보입니다. 슬라이드 제어·스와이프·자동 재생은 적용을 위한 추가 동작입니다.", "status": "recommended", "html": "<section class=\"ri-sample\" data-ri=\"carousel\" aria-labelledby=\"ri-carousel-title\"><div class=\"ri-shell ri-carousel\"><div><h3 class=\"ri-heading\" id=\"ri-carousel-title\">추천사 캐러셀</h3><p class=\"ri-copy\">자동 재생은 기본 꺼짐이며, 사용자가 켠 경우에도 hover, focus, 페이지 숨김, reduced motion에서 멈춥니다.</p></div><div class=\"ri-carousel-viewport\" data-ri-carousel-viewport><div class=\"ri-carousel-track\" data-ri-carousel-track><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"캠페인 단계를 한 화면에서 설명하니 승인 논의가 빨라졌습니다.\"</blockquote><figcaption class=\"ri-cite\">브랜드 마케팅 리드</figcaption></figure><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"명확한 CTA와 짧은 폼 덕분에 모바일 상담 요청 흐름이 단순해졌습니다.\"</blockquote><figcaption class=\"ri-cite\">그로스 매니저</figcaption></figure><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"다크 테마와 FAQ 상태까지 토큰으로 관리할 수 있어 적용 범위가 분명합니다.\"</blockquote><figcaption class=\"ri-cite\">프로덕트 디자이너</figcaption></figure></div></div><div class=\"ri-carousel-controls\"><div class=\"ri-button-row\"><button class=\"ri-button\" type=\"button\" data-ri-prev aria-label=\"이전 추천사\">이전</button><button class=\"ri-button\" type=\"button\" data-ri-next aria-label=\"다음 추천사\">다음</button><button class=\"ri-button\" type=\"button\" data-ri-autoplay aria-pressed=\"false\">자동 재생 켜기</button></div><div class=\"ri-dot-row\" role=\"tablist\" aria-label=\"추천사 선택\"><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"1번 추천사\" aria-selected=\"true\"></button><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"2번 추천사\" aria-selected=\"false\"></button><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"3번 추천사\" aria-selected=\"false\"></button></div><p class=\"ri-status\" data-ri-carousel-status aria-live=\"polite\">1 / 3</p></div></div></section>", "kind": "interaction"}, {"id": "form", "title": "전문가 문의 · 폼 상태", "summary": "2열 입력과 선 그래픽. 필수값·이메일 검증, 로딩·성공·실패·초기화를 한 화면에서 확인하세요.", "sourceTime": "00:27–00:32", "evidence": "00:27–00:32에 문의 폼 구성이 관찰됩니다. 검증·pending·success·failure 상태는 로컬 시뮬레이션이며 실제 서버 동작은 미확인입니다.", "status": "video-observed", "html": "<section class=\"ri-sample\" data-ri=\"form\" aria-labelledby=\"ri-form-title\"><div class=\"ri-shell ri-form-grid\"><div class=\"ri-form-aside\"><div><h3 class=\"ri-heading\" id=\"ri-form-title\">전문가 상담 폼</h3><p class=\"ri-copy\">영상에서 문의 폼과 좌측 그래픽 구성이 관찰됩니다. 실제 적용을 위해 필수값 검증, 인라인 오류, 성공/실패/지연 상태를 같은 화면에서 재현합니다.</p></div><div class=\"ri-line-map\" aria-hidden=\"true\"><span class=\"ri-map-path\"></span></div></div><form novalidate><div class=\"ri-field-grid\"><label class=\"ri-field\"><span class=\"ri-label\">이름 *</span><input class=\"ri-input\" name=\"name\" autocomplete=\"name\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-name-error\"><span class=\"ri-error\" id=\"ri-name-error\"></span></label><label class=\"ri-field\"><span class=\"ri-label\">회사 *</span><input class=\"ri-input\" name=\"company\" autocomplete=\"organization\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-company-error\"><span class=\"ri-error\" id=\"ri-company-error\"></span></label><label class=\"ri-field ri-field-full\"><span class=\"ri-label\">이메일 *</span><input class=\"ri-input\" name=\"email\" type=\"email\" autocomplete=\"email\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-email-error\"><span class=\"ri-error\" id=\"ri-email-error\"></span></label><label class=\"ri-field ri-field-full\"><span class=\"ri-label\">데모 결과</span><select class=\"ri-select\" data-ri-form-mode><option value=\"success\">성공</option><option value=\"pending\">지연 후 성공</option><option value=\"failure\">실패</option></select></label></div><div class=\"ri-button-row\" style=\"margin-top:16px\"><button class=\"ri-button ri-button-primary\" type=\"submit\" aria-busy=\"false\">상담 요청</button><button class=\"ri-button\" type=\"reset\">초기화</button></div><p class=\"ri-status\" data-ri-form-status role=\"status\" aria-live=\"polite\">제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.</p></form></div></section>", "kind": "interaction"}, {"id": "faq", "title": "FAQ · 높이 전환", "summary": "질문을 열고 닫으면 답변 높이와 더하기 표시가 바뀝니다. 빠른 연속 클릭과 키보드 조작을 지원합니다.", "sourceTime": "00:31–00:36", "evidence": "00:31–00:36에 펼친 답변과 질문 목록이 보입니다. 실제 클릭 시점과 easing은 미확인으로, 높이 전환은 재구성했습니다.", "status": "video-observed", "html": "<section class=\"ri-sample\" data-ri=\"faq\" aria-labelledby=\"ri-faq-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-faq-title\">FAQ 아코디언</h3><p class=\"ri-copy\">각 질문은 button으로 동작하고 펼침 상태를 aria-expanded로 제공합니다. reduced motion에서는 높이 전환을 제거합니다.</p><div class=\"ri-faq-list\" style=\"margin-top:24px\"><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"true\" aria-controls=\"ri-faq-panel-1\"><span>캠페인 단계 컴포넌트는 어디에 쓰나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-1\"><div class=\"ri-faq-panel-inner\">목표, 대상, 소재, 학습처럼 순서가 중요한 설명을 짧은 탭 또는 단계 콘텐츠로 보여줄 때 사용합니다.</div></div></div><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"false\" aria-controls=\"ri-faq-panel-2\"><span>한 번에 여러 항목을 열 수 있나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-2\"><div class=\"ri-faq-panel-inner\">이 샘플은 여러 항목을 동시에 열 수 있습니다. 제품 정책에 따라 하나만 여는 방식으로 쉽게 바꿀 수 있습니다.</div></div></div><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"false\" aria-controls=\"ri-faq-panel-3\"><span>모션을 줄인 사용자는 어떻게 처리하나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-3\"><div class=\"ri-faq-panel-inner\">prefers-reduced-motion이 켜져 있으면 높이 애니메이션과 회전 모션을 즉시 상태 변경으로 대체합니다.</div></div></div></div></div></section>", "kind": "interaction"}, {"id": "theme", "title": "시스템 · 라이트 · 다크", "summary": "이 샘플의 테마를 별도로 선택하고 저장합니다. 시스템 모드는 기기의 색상 설정을 따릅니다.", "sourceTime": "00:35-00:37 transition observed; exact control unsure", "evidence": "00:35–00:37 이후 페이지 전체가 다크 상태로 바뀝니다. 정확한 제어 클릭은 불명확하며 범위 지정과 저장 정책은 구현 제안입니다.", "status": "video-observed", "html": "<section class=\"ri-sample\" data-ri=\"theme\" data-ri-storage-key=\"reference-interactions-theme-demo\" aria-labelledby=\"ri-theme-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-theme-title\">범위 제한 테마 선택</h3><p class=\"ri-copy\">영상에서는 전역 다크 전환이 관찰됩니다. 이 샘플은 실제 제품 적용을 위해 컨테이너 범위에서 system/light/dark를 안전하게 저장합니다.</p><div class=\"ri-segment-row\" style=\"margin-top:24px\" role=\"group\" aria-label=\"테마 선택\"><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"system\" aria-pressed=\"true\">시스템</button><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"light\" aria-pressed=\"false\">라이트</button><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"dark\" aria-pressed=\"false\">다크</button></div><div class=\"ri-theme-preview\"><p class=\"ri-status\" data-ri-theme-status aria-live=\"polite\">현재 샘플 테마: system</p><div class=\"ri-theme-swatch-row\" aria-hidden=\"true\"><div class=\"ri-theme-swatch\" style=\"background:var(--color-bg)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-surface)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-text)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-action)\"></div></div></div></div></section>", "kind": "interaction"}, {"id": "navigation", "title": "고정 탐색 · 스크롤 연동", "summary": "좌측 메뉴와 콘텐츠 스크롤을 연결합니다. 모바일 메뉴 열기·닫기, Escape, 접힌 하위 항목을 확인하세요.", "sourceTime": "00:00-00:04", "evidence": "본문을 스크롤하는 동안 좌측 탐색이 유지됩니다. 하위 메뉴·모바일 패널·스크롤 활성 추적은 실제 적용을 위한 보완입니다.", "status": "video-observed", "html": "<section class=\"ri-sample\" data-ri=\"navigation\" aria-labelledby=\"ri-nav-demo-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-nav-demo-title\">반응형 사이드바 내비게이션</h3><p class=\"ri-copy\">좌측 탐색은 영상 첫 화면에서 관찰됩니다. 모바일에서는 버튼으로 메뉴를 열고 Escape로 닫으며 포커스를 트리거로 되돌립니다.</p><div class=\"ri-nav-demo\" style=\"margin-top:24px\"><aside class=\"ri-nav-panel\" aria-label=\"샘플 내비게이션\"><button class=\"ri-button ri-nav-toggle\" type=\"button\" data-ri-nav-toggle aria-expanded=\"false\" aria-controls=\"ri-nav-menu\">메뉴</button><nav class=\"ri-nav-menu\" id=\"ri-nav-menu\" data-ri-nav-menu data-open=\"false\"><ul class=\"ri-nav-list\"><li><a class=\"ri-nav-link\" href=\"#overview\" data-ri-nav-link data-title=\"Overview\" data-copy=\"광고 페이지의 메시지와 주요 CTA를 먼저 확인합니다.\" aria-current=\"page\">Overview <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><button class=\"ri-nav-category\" type=\"button\" data-ri-nav-category aria-expanded=\"true\" aria-controls=\"ri-nav-campaigns\">Campaigns <span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><ul class=\"ri-nav-sublist\" id=\"ri-nav-campaigns\"><li><a class=\"ri-nav-link\" href=\"#goals\" data-ri-nav-link data-title=\"Goals\" data-copy=\"목표 기반 캠페인 단계를 선택합니다.\">Goals <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><a class=\"ri-nav-link\" href=\"#formats\" data-ri-nav-link data-title=\"Formats\" data-copy=\"광고 형식과 예시 카드를 검토합니다.\">Formats <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li></ul></li><li><button class=\"ri-nav-category\" type=\"button\" data-ri-nav-category aria-expanded=\"false\" aria-controls=\"ri-nav-support\">Support <span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><ul class=\"ri-nav-sublist\" id=\"ri-nav-support\"><li><a class=\"ri-nav-link\" href=\"#contact\" data-ri-nav-link data-title=\"Contact\" data-copy=\"전문가 상담 요청 폼으로 이동합니다.\">Contact <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><a class=\"ri-nav-link\" href=\"#faq\" data-ri-nav-link data-title=\"FAQ\" data-copy=\"자주 묻는 질문을 펼쳐 확인합니다.\">FAQ <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li></ul></li></ul></nav></aside><div class=\"ri-nav-content\"><p class=\"ri-panel-kicker\">Active section</p><h4 class=\"ri-panel-title\" data-ri-nav-title>Overview</h4><p class=\"ri-panel-copy\" data-ri-nav-copy>광고 페이지의 메시지와 주요 CTA를 먼저 확인합니다.</p><div class=\"ri-nav-preview\" data-ri-nav-preview tabindex=\"0\" aria-label=\"섹션 스크롤 미리보기\"><section class=\"ri-nav-section\" id=\"overview\" data-ri-nav-section><p class=\"ri-panel-kicker\">Overview</p><h5 class=\"ri-panel-title\">큰 메시지와 CTA</h5><p class=\"ri-panel-copy\">첫 화면의 핵심 문장, 보조 설명, 행동 버튼을 같은 위계로 유지합니다.</p></section><section class=\"ri-nav-section\" id=\"goals\" data-ri-nav-section><p class=\"ri-panel-kicker\">Goals</p><h5 class=\"ri-panel-title\">목표 기반 단계</h5><p class=\"ri-panel-copy\">탭과 단계 설명으로 캠페인 설정 흐름을 좁고 선명하게 보여줍니다.</p></section><section class=\"ri-nav-section\" id=\"formats\" data-ri-nav-section><p class=\"ri-panel-kicker\">Formats</p><h5 class=\"ri-panel-title\">광고 형식 카드</h5><p class=\"ri-panel-copy\">형식별 미리보기와 설명을 스크롤 가능한 본문에서 비교합니다.</p></section><section class=\"ri-nav-section\" id=\"contact\" data-ri-nav-section><p class=\"ri-panel-kicker\">Contact</p><h5 class=\"ri-panel-title\">상담 요청</h5><p class=\"ri-panel-copy\">필수 입력, 지연, 성공, 실패 상태를 한 흐름에서 처리합니다.</p></section><section class=\"ri-nav-section\" id=\"faq\" data-ri-nav-section><p class=\"ri-panel-kicker\">FAQ</p><h5 class=\"ri-panel-title\">자주 묻는 질문</h5><p class=\"ri-panel-copy\">긴 페이지 끝에서 질문을 펼치고 닫는 흐름을 제공합니다.</p></section></div></div></div></div></section>", "kind": "interaction"}], "files": {"tokens.css": "/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n", "tokens.scoped.css": "/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n.adver-system {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n.adver-system[data-theme=\"light\"], .adver-system [data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n.adver-system[data-theme=\"dark\"], .adver-system [data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n.adver-system[data-theme=\"system\"], .adver-system [data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n.adver-system {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n", "motion-kit.css": "/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n", "motion-kit.js": "(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n", "interaction-kit.css": ".ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n", "interaction-kit.js": "(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n"}, "standalone": {"hero": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>히어로 포커스 펄스 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>히어로 포커스 펄스</h1><p>가느다란 축과 좌우 대칭 원을 유지하면서 중앙 원이 확장됐다가 돌아옵니다. 다시 재생과 속도로 움직임을 비교하세요.</p><p>00:00–00:05 중앙 검은 원의 확장·축소를 확인했습니다. 선 그리기와 반복 주기 4.8초는 재구성한 제안값입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"hero\" aria-label=\"히어로 포커스 펄스\"><div class=\"rm-sample-head\"><div><h3>히어로 포커스 펄스</h3><p>관찰한 도형과 중앙 확장 효과. 시간과 easing은 재구성입니다.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-stage rm-hero\" aria-hidden=\"true\"><svg class=\"rm-hero-grid\" viewBox=\"0 0 1000 410\" aria-hidden=\"true\"><path class=\"rm-hero-axis\" d=\"M60 205H940M500 35V375\"/><g><circle class=\"rm-hero-ring\" cx=\"270\" cy=\"205\" r=\"112\"/><circle class=\"rm-hero-ring\" cx=\"360\" cy=\"205\" r=\"86\"/><circle class=\"rm-hero-ring\" cx=\"452\" cy=\"205\" r=\"43\"/><circle class=\"rm-hero-ring\" cx=\"548\" cy=\"205\" r=\"43\"/><circle class=\"rm-hero-ring\" cx=\"640\" cy=\"205\" r=\"86\"/><circle class=\"rm-hero-ring\" cx=\"730\" cy=\"205\" r=\"112\"/></g><circle class=\"rm-hero-core rm-loop\" cx=\"500\" cy=\"205\" r=\"14\"/><g class=\"rm-hero-label\"><rect x=\"82\" y=\"52\" width=\"210\" height=\"32\"/><text x=\"96\" y=\"73\">Ideas become connections.</text><rect x=\"702\" y=\"326\" width=\"220\" height=\"32\"/><text x=\"716\" y=\"347\">Connections create impact.</text></g></svg></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "benefits": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>6가지 기하 아이콘 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>6가지 기하 아이콘</h1><p>선 그리기·원 확장·점 이동을 작은 카드에 적용합니다. hover·키보드 포커스·터치로 상태를 바꿔 보세요.</p><p>00:05–00:09에 작은 기하 도형 6종이 보입니다. hover·focus·touch에 따른 반응은 실제 적용을 위한 제안입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"benefits\" aria-label=\"6가지 기하 아이콘\"><div class=\"rm-sample-head\"><div><h3>6가지 기하 아이콘</h3><p>hover·포커스·터치로 도형 반응을 확인하세요.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-benefits-grid\"><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Reach diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><circle data-rm-part=\"1\" cx=\"64\" cy=\"62\" r=\"32\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"124\" cy=\"62\" r=\"20\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"164\" cy=\"62\" r=\"9\" fill=\"currentColor\" opacity=\".28\"/></svg></button><h4>Reach</h4><p>Expanding circles map audience growth.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Targeting diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><rect data-rm-part=\"1\" x=\"38\" y=\"28\" width=\"58\" height=\"68\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"126\" cy=\"62\" r=\"30\" fill=\"none\" stroke=\"currentColor\"/><path data-rm-part=\"3\" d=\"M150 36l34 26-34 26z\" fill=\"currentColor\"/></svg></button><h4>Targeting</h4><p>Simple shapes show narrowed context.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Creative diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><path data-rm-part=\"1\" d=\"M46 88L88 36l42 52z\" fill=\"none\" stroke=\"currentColor\"/><rect data-rm-part=\"2\" x=\"120\" y=\"32\" width=\"54\" height=\"54\" fill=\"currentColor\" opacity=\".18\"/><circle data-rm-part=\"3\" cx=\"148\" cy=\"62\" r=\"18\" fill=\"currentColor\"/></svg></button><h4>Creative</h4><p>Composed pieces preview ad assembly.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Conversation diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><circle data-rm-part=\"1\" cx=\"76\" cy=\"54\" r=\"28\" fill=\"none\" stroke=\"currentColor\"/><path data-rm-part=\"2\" d=\"M104 48h70v36h-70z\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"126\" cy=\"66\" r=\"5\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"146\" cy=\"66\" r=\"5\" fill=\"currentColor\"/></svg></button><h4>Conversation</h4><p>Small responses imply active posts.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Measurement diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><rect data-rm-part=\"1\" x=\"48\" y=\"72\" width=\"28\" height=\"24\" fill=\"currentColor\" opacity=\".2\"/><rect data-rm-part=\"2\" x=\"96\" y=\"48\" width=\"28\" height=\"48\" fill=\"currentColor\" opacity=\".45\"/><rect data-rm-part=\"3\" x=\"144\" y=\"28\" width=\"28\" height=\"68\" fill=\"currentColor\"/><path d=\"M42 96h138\" stroke=\"currentColor\"/></svg></button><h4>Measure</h4><p>Bars step upward from observed metrics.</p></article><article class=\"rm-benefit\"><button type=\"button\" aria-label=\"Launch diagram replay\"><svg viewBox=\"0 0 220 124\" aria-hidden=\"true\"><ellipse data-rm-part=\"1\" cx=\"110\" cy=\"64\" rx=\"72\" ry=\"28\" fill=\"none\" stroke=\"currentColor\"/><circle data-rm-part=\"2\" cx=\"110\" cy=\"64\" r=\"18\" fill=\"currentColor\"/><circle data-rm-part=\"3\" cx=\"178\" cy=\"64\" r=\"8\" fill=\"currentColor\" opacity=\".28\"/></svg></button><h4>Launch</h4><p>An orbit motif connects to the CTA section.</p></article></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "orbit": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>크레딧 궤도 효과 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>크레딧 궤도 효과</h1><p>점선 궤도와 움직이는 위성이 중앙 CTA를 둘러쌉니다. 화면 밖에서는 반복 동작을 멈춥니다.</p><p>00:08–00:11의 크레딧 영역과 점선·실선 궤도를 바탕으로 구성했습니다. 위성 이동·선 그리기의 타이밍은 재구성입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"orbit\" aria-label=\"크레딧 궤도 효과\"><div class=\"rm-sample-head\"><div><h3>크레딧 궤도 효과</h3><p>화면 밖·비활성 탭·일시정지 상태에서는 반복을 멈춥니다.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-stage rm-orbit\"><div class=\"rm-orbit-wrap\"><svg viewBox=\"0 0 620 376\" aria-hidden=\"true\"><ellipse class=\"rm-orbit-ring\" cx=\"310\" cy=\"188\" rx=\"256\" ry=\"118\"/><ellipse class=\"rm-orbit-ring\" cx=\"310\" cy=\"188\" rx=\"196\" ry=\"76\"/><line x1=\"54\" y1=\"188\" x2=\"566\" y2=\"188\" stroke=\"currentColor\" opacity=\".18\"/></svg><span class=\"rm-satellite rm-loop\" aria-hidden=\"true\"></span><div class=\"rm-credit-card\"><span>Ad credit</span><strong>$500</strong><button class=\"rm-button\" type=\"button\">Start campaign</button></div></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "metrics": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>계단형 지표 · 카운트업 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>계단형 지표 · 카운트업</h1><p>높이가 다른 카드가 순서대로 나타나고 숫자가 목표값에 도달합니다. 수치는 예제 데이터입니다.</p><p>00:11–00:13에 계단형 지표 카드가 보입니다. 숫자 카운트업과 등장 시차는 영상에서 확정되지 않은 적용 제안입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"metrics\" aria-label=\"계단형 지표 · 카운트업\"><div class=\"rm-sample-head\"><div><h3>계단형 지표 · 카운트업</h3><p>Entrance and numbers are controlled by the kit API.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-metrics-grid\"><article class=\"rm-metric\" style=\"--rm-height:150px\"><strong data-rm-count=\"42\" data-rm-suffix=\"%\">0</strong><p>More qualified reach</p></article><article class=\"rm-metric\" style=\"--rm-height:198px\"><strong data-rm-count=\"3.8\" data-rm-suffix=\"x\">0</strong><p>Campaign learning speed</p></article><article class=\"rm-metric\" style=\"--rm-height:246px\"><strong data-rm-count=\"1280\" data-rm-prefix=\"+\">0</strong><p>Measured actions</p></article></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "reveal": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>스크롤 순차 등장 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>스크롤 순차 등장</h1><p>뷰포트 진입을 감지해 편집형 카드를 순서대로 보여줍니다. 모션 감소 환경에서는 콘텐츠를 즉시 표시합니다.</p><p>영상에서 긴 페이지의 스크롤은 확인되지만 fade-up은 확정할 수 없습니다. 이 샘플은 선택적으로 적용하는 효과입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"reveal\" aria-label=\"스크롤 순차 등장\"><div class=\"rm-sample-head\"><div><h3>스크롤 순차 등장</h3><p>Scroll-driven entrance for long-form sections.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-reveal-list\"><article class=\"rm-editorial\"><span class=\"rm-index\">01</span><div><h4>Set one clear outcome</h4><p>Keep the first message and first action readable before details appear.</p></div></article><article class=\"rm-editorial\"><span class=\"rm-index\">02</span><div><h4>Show the mechanism</h4><p>Use compact diagrams to explain how targeting, formats, and feedback connect.</p></div></article><article class=\"rm-editorial\"><span class=\"rm-index\">03</span><div><h4>Measure and refine</h4><p>Bring metrics forward with a predictable, restrained motion rhythm.</p></div></article></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "formats": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>6가지 광고 프리뷰 효과 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n/*\n * Reference motion kit.\n * CSS is dependency-free and scoped with the rm- prefix.\n * Timing/easing values are reconstructed recommendations because the source\n * video does not expose exact implementation timings.\n */\n\n:root {\n  --rm-speed-factor: 1;\n  --rm-ease-out: cubic-bezier(.16, 1, .3, 1);\n  --rm-ease-in-out: cubic-bezier(.65, 0, .35, 1);\n}\n\n/* Resolve color aliases on each sample so nested/global themes propagate. */\n.rm-sample {\n  --rm-line: color-mix(in srgb, var(--color-text, #000) 18%, transparent);\n  --rm-soft-line: color-mix(in srgb, var(--color-text, #000) 10%, transparent);\n  --rm-ink: var(--color-text, #000);\n  --rm-paper: var(--color-bg, #fff);\n  --rm-panel: var(--color-surface, #f2f2f2);\n  --rm-muted: var(--color-muted, rgba(0, 0, 0, .6));\n}\n\n.rm-stage,\n.rm-card,\n.rm-format,\n.rm-metric,\n.rm-benefit,\n.rm-editorial {\n  color: var(--color-text, #000);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.rm-stage {\n  position: relative;\n  overflow: hidden;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-sample-head {\n  display: flex;\n  align-items: end;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  margin-block: 0 var(--space-6, 24px);\n}\n\n.rm-sample-head h3 {\n  margin: 0;\n  font-size: 22px;\n  line-height: 1.18;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-sample-head p,\n.rm-card p,\n.rm-benefit p,\n.rm-format p,\n.rm-editorial p {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.rm-button {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n  font: inherit;\n  font-size: 13px;\n  line-height: 20px;\n  cursor: pointer;\n  transform: translateZ(0);\n  transition: transform calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    background-color calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(160ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-button:hover,\n.rm-button:focus-visible {\n  transform: translateY(-1px);\n}\n\n.rm-button:active,\n.rm-button.rm-active {\n  transform: translateY(0) scale(.96);\n}\n\n.rm-loop,\n.rm-anim {\n  animation-duration: calc(var(--rm-duration, 1s) * var(--rm-speed-factor));\n  animation-timing-function: var(--rm-ease-in-out);\n  animation-fill-mode: both;\n}\n\n.rm-is-paused .rm-loop,\n.rm-is-paused .rm-anim,\n[data-rm-inview=\"false\"] .rm-loop {\n  animation-play-state: paused !important;\n}\n\n.rm-reduce-motion .rm-loop,\n.rm-reduce-motion .rm-anim {\n  animation: none !important;\n  transition: none !important;\n}\n\n.rm-hero {\n  min-height: clamp(300px, 48vw, 560px);\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-hero-grid {\n  width: min(760px, 100%);\n  aspect-ratio: 1.85;\n}\n\n.rm-orbit-ring,\n.rm-hero-ring {\n  fill: none;\n  stroke: var(--rm-ink);\n  stroke-width: 1;\n  vector-effect: non-scaling-stroke;\n}\n\n.rm-hero-ring {\n  transform-origin: 50% 50%;\n  opacity: .82;\n}\n\n.rm-hero-ring:nth-of-type(2n) {\n  opacity: .34;\n}\n\n.rm-hero-dot,\n.rm-hero-core {\n  fill: var(--rm-ink);\n  transform-origin: center;\n}\n\n.rm-hero .rm-loop {\n  --rm-duration: 18s;\n  animation-name: rm-spin;\n  animation-iteration-count: infinite;\n  transform-origin: center;\n}\n\n.rm-hero .rm-counter {\n  --rm-duration: 24s;\n  animation-direction: reverse;\n}\n\n.rm-hero-core {\n  --rm-duration: 2.8s;\n  animation-name: rm-pulse;\n  animation-iteration-count: infinite;\n}\n\n.rm-benefits-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-6, 24px);\n}\n\n.rm-benefit {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-block-start: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-benefit button {\n  width: 100%;\n  min-height: 124px;\n  border: 0;\n  border-radius: 0;\n  padding: 0;\n  background: var(--color-surface, #f2f2f2);\n  color: inherit;\n  cursor: pointer;\n}\n\n.rm-benefit svg {\n  display: block;\n  width: 100%;\n  height: 124px;\n}\n\n.rm-benefit h4,\n.rm-format h4,\n.rm-editorial h4 {\n  margin: 0;\n  font-size: 15px;\n  line-height: 1.35;\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-benefit:focus-within button,\n.rm-benefit:hover button {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.rm-benefit [data-rm-part] {\n  transition: transform calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(220ms * var(--rm-speed-factor)) var(--rm-ease-out);\n  transform-box: fill-box;\n  transform-origin: center;\n}\n\n.rm-benefit:hover [data-rm-part],\n.rm-benefit:focus-within [data-rm-part],\n.rm-benefit.rm-active [data-rm-part] {\n  transform: translateY(-5px) scale(1.04);\n}\n\n.rm-benefit:hover [data-rm-part=\"2\"],\n.rm-benefit:focus-within [data-rm-part=\"2\"],\n.rm-benefit.rm-active [data-rm-part=\"2\"] {\n  transform: translateY(5px) scale(.96);\n}\n\n.rm-orbit {\n  min-height: 360px;\n  display: grid;\n  place-items: center;\n  padding: clamp(24px, 5vw, 72px);\n}\n\n.rm-orbit-wrap {\n  position: relative;\n  width: min(620px, 100%);\n  aspect-ratio: 1.65;\n}\n\n.rm-orbit-wrap svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.rm-credit-card {\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: min(250px, 54%);\n  min-height: 138px;\n  padding: 20px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translate(-50%, -50%);\n  display: grid;\n  align-content: space-between;\n  gap: 18px;\n}\n\n.rm-credit-card strong {\n  font-size: 32px;\n  line-height: 1;\n  letter-spacing: var(--tracking-heading, -.32px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-satellite {\n  --rm-duration: 9s;\n  position: absolute;\n  inset: 50% auto auto 50%;\n  width: 18px;\n  height: 18px;\n  border-radius: 50%;\n  background: var(--color-text, #000);\n  transform: translate(-50%, -50%);\n  animation-name: rm-ellipse-orbit;\n  animation-iteration-count: infinite;\n}\n\n.rm-metrics-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  align-items: end;\n  gap: var(--space-3, 12px);\n  min-height: 248px;\n}\n\n.rm-metric {\n  min-height: var(--rm-height, 160px);\n  background: var(--color-surface, #f2f2f2);\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: var(--space-5, 20px) var(--space-4, 16px);\n  display: grid;\n  align-content: end;\n  gap: 8px;\n  transform: translateY(26px);\n  opacity: 0;\n}\n\n.rm-metric.rm-entered {\n  transform: translateY(0);\n  opacity: 1;\n  transition: transform calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(520ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-metric strong {\n  font-size: clamp(34px, 6vw, 64px);\n  line-height: .95;\n  letter-spacing: var(--tracking-display, -.48px);\n  font-weight: var(--font-weight-medium, 500);\n}\n\n.rm-reveal-list {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.rm-editorial {\n  display: grid;\n  grid-template-columns: minmax(120px, 1fr) minmax(0, 1.6fr);\n  gap: var(--space-6, 24px);\n  padding: var(--space-6, 24px) 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  opacity: 0;\n  transform: translateY(24px);\n}\n\n.rm-editorial.rm-entered {\n  opacity: 1;\n  transform: translateY(0);\n  transition: transform calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out),\n    opacity calc(560ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-editorial .rm-index {\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;\n  font-size: 11px;\n}\n\n.rm-formats-grid {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: var(--space-4, 16px);\n}\n\n.rm-format {\n  min-width: 0;\n  border-top: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding-top: var(--space-4, 16px);\n  display: grid;\n  gap: var(--space-3, 12px);\n}\n\n.rm-format-demo {\n  position: relative;\n  overflow: hidden;\n  min-height: 168px;\n  background: var(--color-surface, #f2f2f2);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-format-demo > * {\n  position: absolute;\n}\n\n.rm-image-reveal .rm-photo {\n  inset: 20px;\n  background: linear-gradient(135deg, var(--color-bg, #fff), var(--color-surface-hover, #ebebeb));\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-image-reveal .rm-mask {\n  inset: 0;\n  background: var(--color-text, #000);\n  transform: translateX(-102%);\n  opacity: .08;\n}\n\n.rm-image-reveal.rm-entered .rm-mask {\n  animation: rm-image-wipe calc(1500ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-video-progress .rm-video-box {\n  inset: 18px 18px 42px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n}\n\n.rm-video-progress .rm-play {\n  inset: 50% auto auto 50%;\n  width: 42px;\n  height: 42px;\n  border-radius: 50%;\n  transform: translate(-50%, -50%);\n  background: var(--color-text, #000);\n}\n\n.rm-video-progress .rm-progress {\n  left: 18px;\n  right: 18px;\n  bottom: 20px;\n  height: 4px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-video-progress .rm-progress::before {\n  content: \"\";\n  display: block;\n  width: 100%;\n  height: 100%;\n  background: var(--color-text, #000);\n  transform-origin: left;\n  transform: scaleX(0);\n  animation: rm-progress calc(3600ms * var(--rm-speed-factor)) linear infinite;\n}\n\n.rm-carousel .rm-slide {\n  inset: 18px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  display: grid;\n  place-items: center;\n  font-size: 40px;\n  line-height: 1;\n}\n\n.rm-promoted .rm-post {\n  inset: 22px;\n  background: var(--color-bg, #fff);\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  padding: 18px;\n  display: grid;\n  gap: 9px;\n  align-content: center;\n  transition: transform calc(240ms * var(--rm-speed-factor)) var(--rm-ease-out);\n}\n\n.rm-promoted:hover .rm-post,\n.rm-promoted:focus-within .rm-post {\n  transform: translateY(-8px);\n}\n\n.rm-line-a,\n.rm-line-b,\n.rm-line-c {\n  position: static;\n  height: 8px;\n  background: var(--color-border, rgba(0, 0, 0, .1));\n}\n\n.rm-line-a {\n  width: 74%;\n}\n\n.rm-line-b {\n  width: 52%;\n}\n\n.rm-line-c {\n  width: 86%;\n}\n\n.rm-product-cycle .rm-product {\n  inset: 24px auto auto 50%;\n  width: 72px;\n  height: 72px;\n  border: 1px solid var(--color-border, rgba(0, 0, 0, .1));\n  background: var(--color-bg, #fff);\n  transform: translateX(-50%);\n  animation: rm-product-cycle calc(3300ms * var(--rm-speed-factor)) var(--rm-ease-in-out) infinite;\n}\n\n.rm-product-cycle .rm-price {\n  left: 24px;\n  right: 24px;\n  bottom: 26px;\n  display: flex;\n  justify-content: space-between;\n  color: var(--color-muted, rgba(0, 0, 0, .6));\n  font-size: 12px;\n}\n\n.rm-boost-counts .rm-count {\n  inset: 50% auto auto 50%;\n  transform: translate(-50%, -50%);\n  font-size: 54px;\n  line-height: 1;\n  letter-spacing: var(--tracking-display, -.48px);\n}\n\n.rm-boost-counts .rm-ping {\n  inset: 50% auto auto 50%;\n  width: 112px;\n  height: 112px;\n  border: 1px solid var(--color-text, #000);\n  border-radius: 50%;\n  transform: translate(-50%, -50%) scale(.5);\n  opacity: 0;\n  animation: rm-ping calc(1800ms * var(--rm-speed-factor)) var(--rm-ease-out) infinite;\n}\n\n@keyframes rm-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n@keyframes rm-pulse {\n  0%,\n  100% {\n    transform: scale(1);\n    opacity: 1;\n  }\n  50% {\n    transform: scale(1.32);\n    opacity: .62;\n  }\n}\n\n@keyframes rm-ellipse-orbit {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(218px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 112px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-218px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -112px) scale(.84);\n  }\n}\n\n@keyframes rm-image-wipe {\n  0%,\n  26% {\n    transform: translateX(-102%);\n  }\n  55%,\n  78% {\n    transform: translateX(0);\n  }\n  100% {\n    transform: translateX(102%);\n  }\n}\n\n@keyframes rm-progress {\n  0% {\n    transform: scaleX(0);\n  }\n  88%,\n  100% {\n    transform: scaleX(1);\n  }\n}\n\n@keyframes rm-product-cycle {\n  0%,\n  22% {\n    border-radius: 50%;\n    transform: translateX(-50%) rotate(0deg);\n  }\n  34%,\n  58% {\n    border-radius: 8px;\n    transform: translateX(-50%) rotate(45deg);\n  }\n  70%,\n  100% {\n    border-radius: 0;\n    transform: translateX(-50%) rotate(90deg);\n  }\n}\n\n@keyframes rm-ping {\n  0% {\n    transform: translate(-50%, -50%) scale(.44);\n    opacity: .42;\n  }\n  100% {\n    transform: translate(-50%, -50%) scale(1.12);\n    opacity: 0;\n  }\n}\n\n@media (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@keyframes rm-ellipse-orbit-small {\n  0% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n  25% {\n    transform: translate(-50%, -50%) translate(128px, 0) scale(1);\n  }\n  50% {\n    transform: translate(-50%, -50%) translate(0, 76px) scale(.84);\n  }\n  75% {\n    transform: translate(-50%, -50%) translate(-128px, 0) scale(1);\n  }\n  100% {\n    transform: translate(-50%, -50%) translate(0, -76px) scale(.84);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .rm-loop,\n  .rm-anim,\n  .rm-image-reveal.rm-entered .rm-mask,\n  .rm-video-progress .rm-progress::before,\n  .rm-product-cycle .rm-product,\n  .rm-boost-counts .rm-ping {\n    animation: none !important;\n    transition: none !important;\n  }\n\n  .rm-metric,\n  .rm-editorial {\n    opacity: 1;\n    transform: none;\n  }\n}\n\n/* Reference hero: symmetric circles stay still; the focal disc expands. */\n.rm-hero-grid { width: 100%; max-width: 1000px; aspect-ratio: 1000 / 410; }\n.rm-hero .rm-hero-core { --rm-duration: 4.8s; animation-name: rm-focus-pulse; animation-iteration-count: infinite; transform-box: fill-box; transform-origin: center; }\n.rm-hero-axis { fill: none; stroke: var(--rm-ink); stroke-width: 1; vector-effect: non-scaling-stroke; }\n.rm-hero-label rect { fill: var(--color-surface); stroke: var(--rm-ink); stroke-width: 1; }\n.rm-hero-label text { fill: var(--rm-ink); font: 13px var(--font-body); }\n@keyframes rm-focus-pulse { 0%, 20%, 65%, 100% { transform: scale(1); } 40%, 44% { transform: scale(4.4); } }\n@media(max-width:600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n/* Components must not rely on a host application's global reset. */\n.rm-sample, .rm-sample *, .rm-sample *::before, .rm-sample *::after { box-sizing: border-box; }\n.rm-sample { color: var(--color-text, #000); font: var(--font-size-body, 16px)/var(--line-height-body, 24px) var(--font-body, system-ui, sans-serif); }\n.rm-sample :focus-visible { outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc); outline-offset: var(--focus-ring-offset, 2px); }\n\n.rm-js-stopped .rm-progress::before { animation: none !important; }\n\n/* React to the component column width inside a wider application. */\n.rm-sample { container-type: inline-size; container-name: rm-sample; min-width: 0; }\n@container rm-sample (max-width: 760px) {\n  .rm-sample-head {\n    align-items: start;\n    flex-direction: column;\n  }\n\n  .rm-benefits-grid,\n  .rm-formats-grid,\n  .rm-metrics-grid {\n    grid-template-columns: 1fr;\n  }\n\n  .rm-metrics-grid {\n    min-height: auto;\n  }\n\n  .rm-metric {\n    min-height: 132px;\n  }\n\n  .rm-editorial {\n    grid-template-columns: 1fr;\n    gap: var(--space-2, 8px);\n  }\n\n  .rm-satellite {\n    animation-name: rm-ellipse-orbit-small;\n  }\n}\n\n@container rm-sample (max-width: 600px) { .rm-hero-label { display:none; } .rm-hero { padding:16px; min-height:220px; } }\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>6가지 광고 프리뷰 효과</h1><p>이미지 공개, 재생 진행률, 슬라이드, 카드 반응, 상품 변경, 숫자 피드백을 코드로 재생합니다.</p><p>00:19–00:27의 광고 형식 그리드를 바탕으로 구성했습니다. 각 프리뷰 루프·hover·카운트는 원본 동작이 아닌 적용 예시입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"rm-sample\" data-rm=\"formats\" aria-label=\"6가지 광고 프리뷰 효과\"><div class=\"rm-sample-head\"><div><h3>6가지 광고 프리뷰 효과</h3><p>Six clear demo states with loops, hover, and numeric updates.</p></div><button class=\"rm-button\" type=\"button\" data-rm-replay>다시 재생</button></div><div class=\"rm-formats-grid\"><article class=\"rm-format\"><div class=\"rm-format-demo rm-image-reveal\"><span class=\"rm-photo\"></span><span class=\"rm-mask\"></span></div><h4>Image reveal</h4><p>Mask pass exposes creative.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-video-progress\"><span class=\"rm-video-box\"></span><span class=\"rm-play\"></span><span class=\"rm-progress\"></span></div><h4>Video progress</h4><p>Progress bar loops while visible.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-carousel\"><span class=\"rm-slide\">01</span></div><h4>Carousel slide</h4><p>Sequential panels rotate with WAAPI fallback.</p></article><article class=\"rm-format\"><button type=\"button\" class=\"rm-format-demo rm-promoted\" aria-label=\"Promoted post lift\"><span class=\"rm-post\"><span class=\"rm-line-a\"></span><span class=\"rm-line-b\"></span><span class=\"rm-line-c\"></span></span></button><h4>Promoted post</h4><p>Post lifts on hover, focus, or touch.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-product-cycle\"><span class=\"rm-product\"></span><span class=\"rm-price\"><span>Product</span><strong>$48</strong></span></div><h4>Product cycle</h4><p>Shape changes suggest catalog rotation.</p></article><article class=\"rm-format\"><div class=\"rm-format-demo rm-boost-counts\"><span class=\"rm-ping\"></span><span class=\"rm-count\">+1,240</span></div><h4>Boost counts</h4><p>Live-style count increments while active.</p></article></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  'use strict';\n\n  var mountedRoots = new Map();\n  var controllers = new Set();\n  var globalPaused = false;\n  var speed = 1;\n  var reduceQuery = window.matchMedia ? window.matchMedia('(prefers-reduced-motion: reduce)') : null;\n  var reduced = reduceQuery ? reduceQuery.matches : false;\n  var hidden = document.hidden;\n\n  function clampSpeed(value) {\n    var next = Number(value);\n    if (!Number.isFinite(next)) return 1;\n    return Math.min(4, Math.max(.25, next));\n  }\n\n  function speedFactor() {\n    return String(1 / speed);\n  }\n\n  function effectivePaused(controller) {\n    return globalPaused || hidden || controller.inView === false;\n  }\n\n  function applyGlobalState() {\n    document.documentElement.classList.toggle('rm-is-paused', globalPaused || hidden);\n    document.documentElement.classList.toggle('rm-reduce-motion', reduced);\n    document.documentElement.style.setProperty('--rm-speed-factor', speedFactor());\n    controllers.forEach(function (controller) {\n      controller.apply();\n    });\n  }\n\n  function makeController(root, node) {\n    var controller = {\n      root: root,\n      node: node,\n      owners: new Set(),\n      cleanups: [],\n      animations: [],\n      timers: [],\n      inView: true,\n      loop: Boolean(node.querySelector('.rm-loop')),\n      active: true,\n      apply: function () {\n        if (!controller.active) return;\n        var paused = effectivePaused(controller);\n        controller.timers.slice().forEach(function (handle) { if (handle.retime) handle.retime(); });\n        node.dataset.rmInview = String(controller.inView !== false);\n        controller.animations.forEach(function (animation) {\n          if (!animation) return;\n          if (reduced) {\n            if (animation.finish) {\n              try { animation.finish(); } catch (error) {}\n            }\n            return;\n          }\n          if (animation.playbackRate !== undefined) animation.playbackRate = speed;\n          if (paused && animation.pause) animation.pause();\n          if (!paused && animation.play && animation.playState === 'paused') animation.play();\n        });\n      },\n      cleanup: function () {\n        if (!controller.active) return;\n        controller.active = false;\n        controller.cleanups.forEach(function (cleanup) { cleanup(); });\n        controller.timers.forEach(function (timer) {\n          timer.active = false;\n          if (timer.raf) cancelAnimationFrame(timer.id);\n          else {\n            clearTimeout(timer.id);\n            clearInterval(timer.id);\n          }\n        });\n        controller.animations.slice().forEach(function (animation) {\n          if (animation && animation.cancel) animation.cancel();\n        });\n        stopCssLoops(controller.node);\n        controller.cleanups = [];\n        controller.timers = [];\n        controller.animations = [];\n        controller.owners.forEach(function (entry) {\n          var index = entry.controllers.indexOf(controller);\n          if (index >= 0) entry.controllers.splice(index, 1);\n        });\n        controller.owners.clear();\n        controllers.delete(controller);\n      }\n    };\n    controllers.add(controller);\n    return controller;\n  }\n\n  function on(target, type, handler, options, controller) {\n    target.addEventListener(type, handler, options);\n    controller.cleanups.push(function () {\n      target.removeEventListener(type, handler, options);\n    });\n  }\n\n  function timer(controller, fn, delay, repeat) {\n    var remaining = reduced ? 0 : delay;\n    var previousTime = performance.now();\n    var previousSpeed = speed;\n    var wasPaused = effectivePaused(controller);\n    var handle = { id: 0, active: true, retime: schedule };\n    function schedule() {\n      clearTimeout(handle.id);\n      if (!handle.active || !controller.active) return;\n      var now = performance.now();\n      if (!wasPaused) remaining -= (now - previousTime) * previousSpeed;\n      previousTime = now;\n      previousSpeed = speed;\n      wasPaused = effectivePaused(controller);\n      if (reduced) remaining = 0;\n      handle.id = setTimeout(run, wasPaused && !reduced ? 100 : Math.max(0, remaining / speed));\n    }\n    function run() {\n      if (!handle.active || !controller.active) return;\n      if (!reduced && effectivePaused(controller)) { schedule(); return; }\n      if (!repeat) {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n      fn();\n      if (repeat && handle.active && controller.active) {\n        remaining = delay;\n        previousTime = performance.now();\n        previousSpeed = speed;\n        wasPaused = effectivePaused(controller);\n        schedule();\n      }\n    }\n    controller.timers.push(handle);\n    schedule();\n    return handle;\n  }\n\n  function animate(controller, element, keyframes, options, fallback) {\n    if (!element) return null;\n    var opts = Object.assign({}, options);\n    if (element.animate && !reduced) {\n      var animation = element.animate(keyframes, opts);\n      animation.playbackRate = speed;\n      controller.animations.push(animation);\n      var settled = false;\n      var remove = function () {\n        var index = controller.animations.indexOf(animation);\n        if (index >= 0) controller.animations.splice(index, 1);\n      };\n      var finish = function () {\n        if (settled) return;\n        settled = true;\n        commitFinalStyles(animation, element, keyframes);\n        if (animation.cancel) animation.cancel();\n        remove();\n      };\n      var cancel = function () {\n        if (settled) return;\n        settled = true;\n        remove();\n      };\n      if (animation.addEventListener) {\n        animation.addEventListener('finish', finish, { once: true });\n        animation.addEventListener('cancel', cancel, { once: true });\n      } else {\n        animation.onfinish = finish;\n        animation.oncancel = cancel;\n      }\n      controller.apply();\n      return animation;\n    }\n    if (typeof fallback === 'function') fallback(element);\n    return null;\n  }\n\n  function commitFinalStyles(animation, element, keyframes) {\n    if (animation.commitStyles) {\n      try {\n        animation.commitStyles();\n        return;\n      } catch (error) {}\n    }\n    var finalFrame = keyframes && keyframes[keyframes.length - 1];\n    if (!finalFrame) return;\n    Object.keys(finalFrame).forEach(function (property) {\n      if (property === 'offset' || property === 'easing' || property === 'composite') return;\n      element.style[property] = finalFrame[property];\n    });\n  }\n\n  function loopElements(root) {\n    return Array.prototype.slice.call(root.querySelectorAll('.rm-loop, .rm-anim, .rm-product, .rm-ping, .rm-mask'));\n  }\n\n  function stopCssLoops(root) {\n    if (root.classList) root.classList.add('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = 'none';\n    });\n  }\n\n  function restoreCssLoops(root) {\n    if (root.classList) root.classList.remove('rm-js-stopped');\n    loopElements(root).forEach(function (element) {\n      element.style.animation = '';\n    });\n  }\n\n  function drawSvgLines(controller, scope, selector, stagger) {\n    var lines = Array.prototype.slice.call(scope.querySelectorAll(selector));\n    lines.forEach(function (line, index) {\n      if (!line.getTotalLength) return;\n      var length = Math.ceil(line.getTotalLength());\n      line.style.strokeDasharray = String(length);\n      line.style.strokeDashoffset = String(length);\n      timer(controller, function () {\n        animate(controller, line, [\n          { strokeDashoffset: length, opacity: .18 },\n          { strokeDashoffset: 0, opacity: getComputedStyle(line).opacity || 1 }\n        ], {\n          duration: 760,\n          easing: 'cubic-bezier(.16,1,.3,1)',\n          fill: 'forwards'\n        }, function () {\n          line.style.strokeDashoffset = '0';\n        });\n      }, reduced ? 0 : index * stagger, false);\n    });\n  }\n\n  function observeLoop(controller) {\n    if (!controller.loop || !('IntersectionObserver' in window)) return;\n    var observer = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        controller.inView = entry.isIntersecting;\n        controller.apply();\n      });\n    }, { threshold: .08 });\n    observer.observe(controller.node);\n    controller.cleanups.push(function () { observer.disconnect(); });\n  }\n\n  function triggerActive(controller, node) {\n    node.classList.remove('rm-active');\n    void node.offsetWidth;\n    node.classList.add('rm-active');\n    timer(controller, function () { node.classList.remove('rm-active'); }, reduced ? 0 : 420, false);\n  }\n\n  function mountHero(controller) {\n    var core = controller.node.querySelector('.rm-hero-core');\n    drawSvgLines(controller, controller.node, '.rm-hero-ring', 70);\n    if (reduced && core) {\n      core.style.transform = 'scale(1)';\n      core.style.opacity = '1';\n    }\n    observeLoop(controller);\n  }\n\n  function mountBenefits(controller) {\n    controller.node.querySelectorAll('.rm-benefit').forEach(function (card) {\n      var button = card.querySelector('button');\n      if (!button) return;\n      drawSvgLines(controller, card, 'circle,path,rect,ellipse,line', 42);\n      var activate = function () { triggerActive(controller, card); };\n      on(button, 'click', activate, false, controller);\n      on(button, 'focus', activate, false, controller);\n      on(button, 'touchstart', activate, { passive: true }, controller);\n    });\n  }\n\n  function mountOrbit(controller) {\n    observeLoop(controller);\n    drawSvgLines(controller, controller.node, '.rm-orbit-ring,line', 90);\n    var button = controller.node.querySelector('.rm-credit-card .rm-button');\n    if (!button) return;\n    on(button, 'click', function () {\n      triggerActive(controller, button);\n    }, false, controller);\n  }\n\n  function mountMetrics(controller) {\n    var metrics = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-metric'));\n    var run = function () {\n      metrics.forEach(function (metric, index) {\n        timer(controller, function () {\n          metric.classList.add('rm-entered');\n          countMetric(controller, metric);\n        }, reduced ? 0 : index * 130, false);\n      });\n    };\n    if (reduced) {\n      metrics.forEach(function (metric) {\n        metric.classList.add('rm-entered');\n        countMetric(controller, metric);\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            observer.disconnect();\n            run();\n          }\n        });\n      }, { threshold: .18 });\n      observer.observe(controller.node);\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      run();\n    }\n  }\n\n  function countMetric(controller, metric) {\n    var valueNode = metric.querySelector('[data-rm-count]');\n    if (!valueNode) return;\n    var raw = valueNode.dataset.rmCount;\n    var target = Number(raw);\n    var decimals = Math.min(10, (raw.split('.')[1] || '').length);\n    var suffix = valueNode.dataset.rmSuffix || '';\n    var prefix = valueNode.dataset.rmPrefix || '';\n    var format = function (value) {\n      return decimals ? value.toFixed(decimals) : Math.round(value).toLocaleString('en-US');\n    };\n    var finalValue = prefix + (Number.isFinite(target) ? format(target) : '0') + suffix;\n    if (!Number.isFinite(target) || reduced) {\n      valueNode.textContent = finalValue;\n      return;\n    }\n    var elapsed = 0;\n    var last = performance.now();\n    var handle = { id: 0, raf: true, active: true };\n    controller.timers.push(handle);\n    function frame(now) {\n      if (!controller.active || !handle.active) return;\n      var delta = now - last;\n      last = now;\n      if (!effectivePaused(controller)) elapsed += delta * speed;\n      var progress = reduced ? 1 : Math.min(1, elapsed / 860);\n      var eased = 1 - Math.pow(1 - progress, 3);\n      valueNode.textContent = progress === 1 ? finalValue : prefix + format(target * eased) + suffix;\n      if (progress < 1) handle.id = requestAnimationFrame(frame);\n      else {\n        handle.active = false;\n        var index = controller.timers.indexOf(handle);\n        if (index >= 0) controller.timers.splice(index, 1);\n      }\n    }\n    handle.id = requestAnimationFrame(frame);\n  }\n\n  function mountReveal(controller) {\n    var cards = Array.prototype.slice.call(controller.node.querySelectorAll('.rm-editorial'));\n    var reveal = function (card, index) {\n      timer(controller, function () {\n        card.classList.add('rm-entered');\n      }, reduced ? 0 : index * 90, false);\n    };\n    if (reduced) {\n      cards.forEach(function (card) {\n        card.classList.add('rm-entered');\n      });\n      return;\n    }\n    if ('IntersectionObserver' in window) {\n      var observer = new IntersectionObserver(function (entries) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            reveal(entry.target, cards.indexOf(entry.target));\n            observer.unobserve(entry.target);\n          }\n        });\n      }, { threshold: .2, rootMargin: '0px 0px -8% 0px' });\n      cards.forEach(function (card) { observer.observe(card); });\n      controller.cleanups.push(function () { observer.disconnect(); });\n    } else {\n      cards.forEach(reveal);\n    }\n  }\n\n  function mountFormats(controller) {\n    observeLoop(controller);\n    controller.node.querySelectorAll('.rm-format').forEach(function (format) {\n      format.classList.add('rm-entered');\n      var button = format.querySelector('button');\n      if (button) {\n        on(button, 'click', function () { triggerActive(controller, format); }, false, controller);\n        on(button, 'focus', function () { triggerActive(controller, format); }, false, controller);\n      }\n    });\n    var carousel = controller.node.querySelector('.rm-carousel .rm-slide');\n    if (carousel) runCarousel(controller, carousel);\n    var boost = controller.node.querySelector('.rm-boost-counts .rm-count');\n    if (boost) runBoost(controller, boost);\n  }\n\n  function runCarousel(controller, node) {\n    var values = ['01', '02', '03'];\n    var index = 0;\n    node.textContent = values[index];\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      index = (index + 1) % values.length;\n      node.textContent = values[index];\n      animate(controller, node, [\n        { transform: 'translateX(18px)', opacity: .25 },\n        { transform: 'translateX(0)', opacity: 1 }\n      ], { duration: 260, easing: 'cubic-bezier(.16,1,.3,1)' });\n    }, 1500, true);\n  }\n\n  function runBoost(controller, node) {\n    var count = 1240;\n    node.textContent = '+1,240';\n    if (reduced) return;\n    timer(controller, function () {\n      if (effectivePaused(controller)) return;\n      count += Math.floor(18 + Math.random() * 54);\n      node.textContent = '+' + count.toLocaleString('en-US');\n    }, 1350, true);\n  }\n\n  function mountNode(root, node) {\n    restoreCssLoops(node);\n    var controller = makeController(root, node);\n    node.querySelectorAll('[data-rm-replay]').forEach(function (button) {\n      on(button, 'click', function () { replayNode(controller); }, false, controller);\n    });\n    var type = node.dataset.rm;\n    if (type === 'hero') mountHero(controller);\n    if (type === 'benefits') mountBenefits(controller);\n    if (type === 'orbit') mountOrbit(controller);\n    if (type === 'metrics') mountMetrics(controller);\n    if (type === 'reveal') mountReveal(controller);\n    if (type === 'formats') mountFormats(controller);\n    controller.apply();\n    return controller;\n  }\n\n  function matchingControllers(root) {\n    var matches = [];\n    controllers.forEach(function (controller) {\n      if (controller.node === root || (root.contains && root.contains(controller.node))) {\n        matches.push(controller);\n      }\n    });\n    return matches;\n  }\n\n  function controllerForNode(node) {\n    var found = null;\n    controllers.forEach(function (controller) {\n      if (controller.node === node) found = controller;\n    });\n    return found;\n  }\n\n  function replayNode(controller) {\n    var owners = Array.from(controller.owners).filter(function (entry) { return entry.active; });\n    var node = controller.node;\n    controller.cleanup();\n    if (!owners.length) return;\n    resetScope(node);\n    var next = mountNode(controller.root, node);\n    owners.forEach(function (entry) {\n      next.owners.add(entry);\n      entry.controllers.push(next);\n    });\n    applyGlobalState();\n  }\n\n  function collectNodes(root) {\n    var nodes = [];\n    if (root.matches && root.matches('[data-rm]')) nodes.push(root);\n    root.querySelectorAll('[data-rm]').forEach(function (node) {\n      if (nodes.indexOf(node) === -1) nodes.push(node);\n    });\n    return nodes;\n  }\n\n  function mountNodes(root, entry) {\n    collectNodes(root).forEach(function (node) {\n      var controller = controllerForNode(node) || mountNode(root, node);\n      if (controller.owners.has(entry)) return;\n      controller.owners.add(entry);\n      entry.controllers.push(controller);\n    });\n  }\n\n  function resetScope(root) {\n    restoreCssLoops(root);\n    var activeNodes = [];\n    if (root.matches && (root.matches('.rm-entered') || root.matches('.rm-active'))) activeNodes.push(root);\n    root.querySelectorAll('.rm-entered, .rm-active').forEach(function (node) {\n      if (activeNodes.indexOf(node) === -1) activeNodes.push(node);\n    });\n    activeNodes.forEach(function (node) {\n      node.classList.remove('rm-entered', 'rm-active');\n    });\n    root.querySelectorAll('[data-rm-count]').forEach(function (node) {\n      node.textContent = (node.dataset.rmPrefix || '') + '0' + (node.dataset.rmSuffix || '');\n    });\n  }\n\n  function mount(root) {\n    root = root || document;\n    if (mountedRoots.has(root)) {\n      var current = mountedRoots.get(root);\n      mountNodes(root, current);\n      applyGlobalState();\n      return current.cleanup;\n    }\n    var entry = {\n      active: true,\n      controllers: [],\n      cleanup: function () {\n        if (!entry.active) return;\n        entry.active = false;\n        entry.controllers.slice().forEach(function (controller) {\n          controller.owners.delete(entry);\n          if (!controller.owners.size) controller.cleanup();\n        });\n        entry.controllers = [];\n        if (mountedRoots.get(root) === entry) mountedRoots.delete(root);\n      }\n    };\n    mountNodes(root, entry);\n    mountedRoots.set(root, entry);\n    applyGlobalState();\n    return entry.cleanup;\n  }\n\n  function replay(root) {\n    root = root || document;\n    var existing = matchingControllers(root);\n    if (existing.length) existing.slice().forEach(replayNode);\n    else return mount(root);\n  }\n\n  function setPaused(value) {\n    globalPaused = Boolean(value);\n    applyGlobalState();\n  }\n\n  function setSpeed(value) {\n    speed = clampSpeed(value);\n    applyGlobalState();\n  }\n\n  function handleReducedMotion(event) {\n    reduced = Boolean(event.matches);\n    Array.from(controllers).forEach(replayNode);\n    applyGlobalState();\n  }\n\n  function handleVisibility() {\n    hidden = document.hidden;\n    applyGlobalState();\n  }\n\n  if (reduceQuery) {\n    if (reduceQuery.addEventListener) reduceQuery.addEventListener('change', handleReducedMotion);\n    else if (reduceQuery.addListener) reduceQuery.addListener(handleReducedMotion);\n  }\n  document.addEventListener('visibilitychange', handleVisibility);\n  applyGlobalState();\n\n  window.ReferenceMotion = {\n    mount: mount,\n    replay: replay,\n    setPaused: setPaused,\n    setSpeed: setSpeed\n  };\n})();\n<\/script><script>\nconst api = window.ReferenceMotion;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "buttons": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>버튼 · 상태 피드백 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>버튼 · 상태 피드백</h1><p>hover·pressed·focus·disabled와 요청 중·완료 상태를 직접 확인하세요. 원격 요청 없이 동작합니다.</p><p>pill CTA를 바탕으로 사용 상태를 보완했습니다. hover·로딩·완료 피드백은 영상에서 확인되지 않은 적용 제안입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"buttons\" aria-labelledby=\"ri-buttons-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-buttons-title\">버튼 상태</h3><p class=\"ri-copy\">기본 CTA는 44px 이상 터치 영역, 포커스 링, hover 화살표 이동, 눌림 상태, 비활성 상태, 로컬 loading-to-confirm 피드백을 포함합니다.</p><div class=\"ri-button-row\" style=\"margin-top:24px\"><button class=\"ri-button ri-button-primary\" type=\"button\" data-ri-button-action aria-busy=\"false\">확인 요청 <span class=\"ri-button-arrow\" aria-hidden=\"true\">-&gt;</span></button><button class=\"ri-button\" type=\"button\">보조 행동 <span class=\"ri-button-arrow\" aria-hidden=\"true\">-&gt;</span></button><button class=\"ri-button\" type=\"button\" aria-pressed=\"true\">선택됨</button><button class=\"ri-button\" type=\"button\" disabled>비활성</button></div><p class=\"ri-status\" data-ri-button-status role=\"status\" aria-live=\"polite\">버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.</p></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "tabs": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>캠페인 4단계 탭 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>캠페인 4단계 탭</h1><p>선택한 단계에 따라 패널과 밑줄이 전환됩니다. 방향키·Home·End로도 탐색할 수 있습니다.</p><p>00:14–00:18에 선택 단계와 패널 변화가 보입니다. 이동 밑줄과 240ms 전환은 제안값이며 파랑은 패널 내부 콘텐츠에만 사용합니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"tabs\" aria-labelledby=\"ri-tabs-title\"><div class=\"ri-shell ri-tabs-layout\"><div><h3 class=\"ri-heading\" id=\"ri-tabs-title\">캠페인 단계 탭</h3><p class=\"ri-copy\">탭 전환은 선택 상태, 얇은 underline 위치, 패널 카피와 mock panel을 함께 갱신합니다. 키보드는 좌우 방향키, Home, End를 지원합니다.</p></div><div class=\"ri-tabs-list\" role=\"tablist\" aria-label=\"캠페인 단계\"><button class=\"ri-tab\" id=\"ri-tab-1\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"true\" data-kicker=\"01 / Objective\" data-title=\"목표를 먼저 정하세요.\" data-copy=\"한 가지 캠페인 목표를 정하면 메시지, 예산, 측정 기준이 같은 방향으로 정렬됩니다.\"><span class=\"ri-tab-index\">01</span><span class=\"ri-tab-label\">목표</span></button><button class=\"ri-tab\" id=\"ri-tab-2\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"02 / Audience\" data-title=\"도달할 사람을 좁히세요.\" data-copy=\"관심사와 맥락을 기준으로 타깃을 정의하면 더 적은 노출로도 명확한 반응을 얻을 수 있습니다.\"><span class=\"ri-tab-index\">02</span><span class=\"ri-tab-label\">대상</span></button><button class=\"ri-tab\" id=\"ri-tab-3\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"03 / Creative\" data-title=\"짧은 소재를 만드세요.\" data-copy=\"큰 메시지 하나, 읽기 쉬운 보조 문장, 분명한 행동 버튼으로 광고 소재를 구성합니다.\"><span class=\"ri-tab-index\">03</span><span class=\"ri-tab-label\">소재</span></button><button class=\"ri-tab\" id=\"ri-tab-4\" role=\"tab\" aria-controls=\"ri-tab-panel\" aria-selected=\"false\" data-kicker=\"04 / Learn\" data-title=\"결과에서 다음 캠페인을 배우세요.\" data-copy=\"성과를 확인하고 다음 실험으로 이어지는 개선 포인트를 같은 흐름 안에서 기록합니다.\"><span class=\"ri-tab-index\">04</span><span class=\"ri-tab-label\">학습</span></button><span class=\"ri-tabs-indicator\" data-ri-tabs-indicator aria-hidden=\"true\"></span></div><div class=\"ri-tab-panel\" id=\"ri-tab-panel\" role=\"tabpanel\" aria-labelledby=\"ri-tab-1\"><div><p class=\"ri-panel-kicker\" data-ri-panel-kicker>01 / Objective</p><h4 class=\"ri-panel-title\" data-ri-panel-title>목표를 먼저 정하세요.</h4><p class=\"ri-panel-copy\" data-ri-panel-copy>한 가지 캠페인 목표를 정하면 메시지, 예산, 측정 기준이 같은 방향으로 정렬됩니다.</p></div><div class=\"ri-mock-panel\" aria-hidden=\"true\"><div class=\"ri-mock-header\"></div><div class=\"ri-mock-line\"></div><div class=\"ri-mock-line\"></div><div class=\"ri-mock-bars\"><span></span><span></span><span></span><span></span><span></span></div><div class=\"ri-mock-grid\"><div class=\"ri-mock-card\"></div><div class=\"ri-mock-card\"></div></div></div></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "carousel": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>인용 카드 캐러셀 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>인용 카드 캐러셀</h1><p>이전·다음·페이지 점·스와이프로 카드를 탐색합니다. 자동 재생은 직접 켜고 포커스·hover 시 멈춥니다.</p><p>00:11–00:13에 어두운 인용 카드가 보입니다. 슬라이드 제어·스와이프·자동 재생은 적용을 위한 추가 동작입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"carousel\" aria-labelledby=\"ri-carousel-title\"><div class=\"ri-shell ri-carousel\"><div><h3 class=\"ri-heading\" id=\"ri-carousel-title\">추천사 캐러셀</h3><p class=\"ri-copy\">자동 재생은 기본 꺼짐이며, 사용자가 켠 경우에도 hover, focus, 페이지 숨김, reduced motion에서 멈춥니다.</p></div><div class=\"ri-carousel-viewport\" data-ri-carousel-viewport><div class=\"ri-carousel-track\" data-ri-carousel-track><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"캠페인 단계를 한 화면에서 설명하니 승인 논의가 빨라졌습니다.\"</blockquote><figcaption class=\"ri-cite\">브랜드 마케팅 리드</figcaption></figure><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"명확한 CTA와 짧은 폼 덕분에 모바일 상담 요청 흐름이 단순해졌습니다.\"</blockquote><figcaption class=\"ri-cite\">그로스 매니저</figcaption></figure><figure class=\"ri-slide\" data-ri-slide><blockquote class=\"ri-quote\">\"다크 테마와 FAQ 상태까지 토큰으로 관리할 수 있어 적용 범위가 분명합니다.\"</blockquote><figcaption class=\"ri-cite\">프로덕트 디자이너</figcaption></figure></div></div><div class=\"ri-carousel-controls\"><div class=\"ri-button-row\"><button class=\"ri-button\" type=\"button\" data-ri-prev aria-label=\"이전 추천사\">이전</button><button class=\"ri-button\" type=\"button\" data-ri-next aria-label=\"다음 추천사\">다음</button><button class=\"ri-button\" type=\"button\" data-ri-autoplay aria-pressed=\"false\">자동 재생 켜기</button></div><div class=\"ri-dot-row\" role=\"tablist\" aria-label=\"추천사 선택\"><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"1번 추천사\" aria-selected=\"true\"></button><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"2번 추천사\" aria-selected=\"false\"></button><button class=\"ri-dot\" type=\"button\" data-ri-dot role=\"tab\" aria-label=\"3번 추천사\" aria-selected=\"false\"></button></div><p class=\"ri-status\" data-ri-carousel-status aria-live=\"polite\">1 / 3</p></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "form": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>전문가 문의 · 폼 상태 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>전문가 문의 · 폼 상태</h1><p>2열 입력과 선 그래픽. 필수값·이메일 검증, 로딩·성공·실패·초기화를 한 화면에서 확인하세요.</p><p>00:27–00:32에 문의 폼 구성이 관찰됩니다. 검증·pending·success·failure 상태는 로컬 시뮬레이션이며 실제 서버 동작은 미확인입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"form\" aria-labelledby=\"ri-form-title\"><div class=\"ri-shell ri-form-grid\"><div class=\"ri-form-aside\"><div><h3 class=\"ri-heading\" id=\"ri-form-title\">전문가 상담 폼</h3><p class=\"ri-copy\">영상에서 문의 폼과 좌측 그래픽 구성이 관찰됩니다. 실제 적용을 위해 필수값 검증, 인라인 오류, 성공/실패/지연 상태를 같은 화면에서 재현합니다.</p></div><div class=\"ri-line-map\" aria-hidden=\"true\"><span class=\"ri-map-path\"></span></div></div><form novalidate><div class=\"ri-field-grid\"><label class=\"ri-field\"><span class=\"ri-label\">이름 *</span><input class=\"ri-input\" name=\"name\" autocomplete=\"name\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-name-error\"><span class=\"ri-error\" id=\"ri-name-error\"></span></label><label class=\"ri-field\"><span class=\"ri-label\">회사 *</span><input class=\"ri-input\" name=\"company\" autocomplete=\"organization\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-company-error\"><span class=\"ri-error\" id=\"ri-company-error\"></span></label><label class=\"ri-field ri-field-full\"><span class=\"ri-label\">이메일 *</span><input class=\"ri-input\" name=\"email\" type=\"email\" autocomplete=\"email\" data-ri-required required aria-required=\"true\" aria-describedby=\"ri-email-error\"><span class=\"ri-error\" id=\"ri-email-error\"></span></label><label class=\"ri-field ri-field-full\"><span class=\"ri-label\">데모 결과</span><select class=\"ri-select\" data-ri-form-mode><option value=\"success\">성공</option><option value=\"pending\">지연 후 성공</option><option value=\"failure\">실패</option></select></label></div><div class=\"ri-button-row\" style=\"margin-top:16px\"><button class=\"ri-button ri-button-primary\" type=\"submit\" aria-busy=\"false\">상담 요청</button><button class=\"ri-button\" type=\"reset\">초기화</button></div><p class=\"ri-status\" data-ri-form-status role=\"status\" aria-live=\"polite\">제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.</p></form></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "faq": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>FAQ · 높이 전환 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>FAQ · 높이 전환</h1><p>질문을 열고 닫으면 답변 높이와 더하기 표시가 바뀝니다. 빠른 연속 클릭과 키보드 조작을 지원합니다.</p><p>00:31–00:36에 펼친 답변과 질문 목록이 보입니다. 실제 클릭 시점과 easing은 미확인으로, 높이 전환은 재구성했습니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"faq\" aria-labelledby=\"ri-faq-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-faq-title\">FAQ 아코디언</h3><p class=\"ri-copy\">각 질문은 button으로 동작하고 펼침 상태를 aria-expanded로 제공합니다. reduced motion에서는 높이 전환을 제거합니다.</p><div class=\"ri-faq-list\" style=\"margin-top:24px\"><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"true\" aria-controls=\"ri-faq-panel-1\"><span>캠페인 단계 컴포넌트는 어디에 쓰나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-1\"><div class=\"ri-faq-panel-inner\">목표, 대상, 소재, 학습처럼 순서가 중요한 설명을 짧은 탭 또는 단계 콘텐츠로 보여줄 때 사용합니다.</div></div></div><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"false\" aria-controls=\"ri-faq-panel-2\"><span>한 번에 여러 항목을 열 수 있나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-2\"><div class=\"ri-faq-panel-inner\">이 샘플은 여러 항목을 동시에 열 수 있습니다. 제품 정책에 따라 하나만 여는 방식으로 쉽게 바꿀 수 있습니다.</div></div></div><div class=\"ri-faq-item\"><button class=\"ri-faq-button\" type=\"button\" data-ri-faq-button aria-expanded=\"false\" aria-controls=\"ri-faq-panel-3\"><span>모션을 줄인 사용자는 어떻게 처리하나요?</span><span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><div class=\"ri-faq-panel\" id=\"ri-faq-panel-3\"><div class=\"ri-faq-panel-inner\">prefers-reduced-motion이 켜져 있으면 높이 애니메이션과 회전 모션을 즉시 상태 변경으로 대체합니다.</div></div></div></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "theme": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>시스템 · 라이트 · 다크 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>시스템 · 라이트 · 다크</h1><p>이 샘플의 테마를 별도로 선택하고 저장합니다. 시스템 모드는 기기의 색상 설정을 따릅니다.</p><p>00:35–00:37 이후 페이지 전체가 다크 상태로 바뀝니다. 정확한 제어 클릭은 불명확하며 범위 지정과 저장 정책은 구현 제안입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"theme\" data-ri-storage-key=\"reference-interactions-theme-demo\" aria-labelledby=\"ri-theme-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-theme-title\">범위 제한 테마 선택</h3><p class=\"ri-copy\">영상에서는 전역 다크 전환이 관찰됩니다. 이 샘플은 실제 제품 적용을 위해 컨테이너 범위에서 system/light/dark를 안전하게 저장합니다.</p><div class=\"ri-segment-row\" style=\"margin-top:24px\" role=\"group\" aria-label=\"테마 선택\"><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"system\" aria-pressed=\"true\">시스템</button><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"light\" aria-pressed=\"false\">라이트</button><button class=\"ri-button\" type=\"button\" data-ri-theme-choice=\"dark\" aria-pressed=\"false\">다크</button></div><div class=\"ri-theme-preview\"><p class=\"ri-status\" data-ri-theme-status aria-live=\"polite\">현재 샘플 테마: system</p><div class=\"ri-theme-swatch-row\" aria-hidden=\"true\"><div class=\"ri-theme-swatch\" style=\"background:var(--color-bg)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-surface)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-text)\"></div><div class=\"ri-theme-swatch\" style=\"background:var(--color-action)\"></div></div></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>", "navigation": "<!doctype html>\n<html lang=\"ko\"><head><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>고정 탐색 · 스크롤 연동 — Standalone example</title>\n<style>/* Generated by scripts/build-tokens.py from tokens.json. Edit JSON, then rebuild. */\n:root {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n  --font-body: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-display: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, \"Segoe UI\", sans-serif;\n  --font-weight-regular: 400;\n  --font-weight-medium: 500;\n  --space-1: 4px;\n  --space-2: 8px;\n  --space-3: 12px;\n  --space-4: 16px;\n  --space-6: 24px;\n  --space-8: 32px;\n  --space-10: 40px;\n  --space-12: 48px;\n  --space-14: 56px;\n  --space-20: 80px;\n  --font-size-display: 48px;\n  --line-height-display: 52px;\n  --tracking-display: -0.48px;\n  --font-size-heading: 32px;\n  --line-height-heading: 36px;\n  --tracking-heading: -0.32px;\n  --font-size-body: 16px;\n  --line-height-body: 24px;\n  --font-size-label: 13px;\n  --line-height-label: 20px;\n  --tracking-label: 0.13008px;\n  --content-max: 1120px;\n  --button-height-observed-desktop: 24px;\n  --button-height-observed-mobile: 36px;\n  --touch-target-min: 44px;\n  --radius-pill: 9999px;\n  --border-width: 1px;\n  --focus-ring-width: 2px;\n  --focus-ring-offset: 2px;\n  --duration-fast: 150ms;\n  --duration-standard: 200ms;\n}\n\n[data-theme=\"light\"] {\n  --color-bg: #ffffff;\n  --color-surface: #f2f2f2;\n  --color-surface-hover: #ebebeb;\n  --color-text: #000000;\n  --color-muted: rgba(0, 0, 0, 0.6);\n  --color-border: rgba(0, 0, 0, 0.1);\n  --color-action: #000000;\n  --color-action-hover: rgba(0, 0, 0, 0.8);\n  --color-on-action: #ffffff;\n  --color-focus: #005fcc;\n}\n\n[data-theme=\"dark\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n\n@media (prefers-color-scheme: dark) {\n[data-theme=\"system\"] {\n  --color-bg: #000000;\n  --color-surface: #161616;\n  --color-surface-hover: #262626;\n  --color-text: #ffffff;\n  --color-muted: #aaaaaa;\n  --color-border: rgba(255, 255, 255, 0.1882352941);\n  --color-action: #ffffff;\n  --color-action-hover: #dddddd;\n  --color-on-action: #000000;\n  --color-focus: #8ab4ff;\n}\n}\n\n@media (prefers-reduced-motion: reduce) {\n:root {\n  --duration-fast: 0ms;\n  --duration-standard: 0ms;\n}\n}\n\n.ri-sample {\n  container-type: inline-size;\n  container-name: ri-sample;\n  color: var(--color-text, #000);\n  background: var(--color-bg, #fff);\n  font-family: var(--font-body, ui-sans-serif, system-ui, sans-serif);\n}\n\n.ri-sample,\n.ri-sample * {\n  box-sizing: border-box;\n}\n\n.ri-sample button,\n.ri-sample input {\n  font: inherit;\n}\n\n.ri-sample button {\n  min-height: var(--touch-target-min, 44px);\n  cursor: pointer;\n}\n\n.ri-sample button:disabled {\n  cursor: not-allowed;\n}\n\n.ri-sample :focus-visible {\n  outline: var(--focus-ring-width, 2px) solid var(--color-focus, #005fcc);\n  outline-offset: var(--focus-ring-offset, 2px);\n}\n\n.ri-shell {\n  width: min(100%, var(--content-max, 1120px));\n  margin-inline: auto;\n  padding: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-bg, #fff);\n}\n\n.ri-heading {\n  margin: 0 0 var(--space-2, 8px);\n  font-size: var(--font-size-heading, 32px);\n  line-height: var(--line-height-heading, 36px);\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-copy {\n  margin: 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-button-row,\n.ri-segment-row {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n}\n\n.ri-button {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2, 8px);\n  min-height: var(--touch-target-min, 44px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: var(--radius-pill, 9999px);\n  padding: 10px 18px;\n  background: var(--color-surface, #f2f2f2);\n  color: var(--color-text, #000);\n  transition: background-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover {\n  background: var(--color-surface-hover, #ebebeb);\n}\n\n.ri-button:active,\n.ri-button[aria-pressed=\"true\"] {\n  transform: translateY(1px);\n}\n\n.ri-button:disabled,\n.ri-button[aria-busy=\"true\"] {\n  opacity: 0.48;\n}\n\n.ri-button-primary {\n  border-color: var(--color-action, #000);\n  background: var(--color-action, #000);\n  color: var(--color-on-action, #fff);\n}\n\n.ri-button-primary:hover {\n  background: var(--color-action-hover, rgba(0, 0, 0, 0.8));\n}\n\n.ri-button-arrow {\n  display: inline-block;\n  transition: transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-button:hover .ri-button-arrow {\n  transform: translateX(4px);\n}\n\n.ri-spinner {\n  width: 14px;\n  height: 14px;\n  border: 2px solid currentColor;\n  border-right-color: transparent;\n  border-radius: 999px;\n  animation: ri-spin 700ms linear infinite;\n}\n\n@keyframes ri-spin {\n  to {\n    transform: rotate(1turn);\n  }\n}\n\n.ri-tabs-layout {\n  display: grid;\n  gap: var(--space-6, 24px);\n}\n\n.ri-tabs-list,\n.ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n  position: relative;\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-3, 12px);\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-tab,\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"] {\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  background: transparent;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  text-align: left;\n  padding: 0 0 var(--space-3, 12px);\n}\n\n.ri-tab[aria-selected=\"true\"],\n.ri-sample[data-ri=\"tabs\"] [role=\"tab\"][aria-selected=\"true\"] {\n  color: var(--color-text, #000);\n}\n\n.ri-tab-index {\n  display: block;\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-tab-label {\n  display: block;\n  font-size: 13px;\n  line-height: 20px;\n}\n\n.ri-tabs-indicator,\n[data-ri-tabs-indicator] {\n  position: absolute;\n  left: 0;\n  bottom: -1px;\n  width: 0;\n  height: 2px;\n  background: var(--color-text, #000);\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), width calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-tabs-indicator[data-ri-no-transition],\n[data-ri-tabs-indicator][data-ri-no-transition] {\n  transition: none;\n}\n\n.ri-tab-panel,\n.ri-mock-panel {\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-tab-panel {\n  display: grid;\n  grid-template-columns: minmax(0, 0.9fr) minmax(240px, 1.1fr);\n  gap: var(--space-6, 24px);\n  min-height: 260px;\n  padding: var(--space-6, 24px);\n}\n\n.ri-panel-kicker {\n  margin: 0 0 var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: 11px;\n  line-height: 18px;\n}\n\n.ri-panel-title {\n  margin: 0;\n  font-size: 28px;\n  line-height: 34px;\n  font-weight: var(--font-weight-medium, 500);\n  letter-spacing: 0;\n}\n\n.ri-panel-copy {\n  max-width: 38rem;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-body, 16px);\n  line-height: var(--line-height-body, 24px);\n}\n\n.ri-mock-panel {\n  display: grid;\n  gap: var(--space-3, 12px);\n  align-content: center;\n  padding: var(--space-6, 24px);\n  background: var(--color-bg, #fff);\n}\n\n.ri-mock-header,\n.ri-mock-line,\n.ri-mock-card {\n  background: var(--color-surface, #f2f2f2);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-mock-header {\n  height: 52px;\n}\n\n.ri-mock-line {\n  height: 14px;\n}\n\n.ri-mock-line:nth-child(3) {\n  width: 72%;\n}\n\n.ri-mock-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-3, 12px);\n}\n\n.ri-mock-bars {\n  display: grid;\n  gap: 7px;\n  align-items: end;\n  grid-template-columns: repeat(5, 1fr);\n  min-height: 86px;\n  padding-top: var(--space-3, 12px);\n}\n\n.ri-mock-bars span {\n  display: block;\n  min-height: 22px;\n  background: #1d9bf0;\n}\n\n.ri-mock-bars span:nth-child(2) {\n  min-height: 48px;\n}\n\n.ri-mock-bars span:nth-child(3) {\n  min-height: 34px;\n}\n\n.ri-mock-bars span:nth-child(4) {\n  min-height: 66px;\n}\n\n.ri-mock-bars span:nth-child(5) {\n  min-height: 42px;\n}\n\n.ri-mock-card {\n  min-height: 88px;\n}\n\n.ri-entering .ri-panel-title,\n.ri-entering .ri-panel-copy,\n.ri-entering .ri-mock-header,\n.ri-entering .ri-mock-line,\n.ri-entering .ri-mock-card,\n.ri-entering .ri-mock-bars span {\n  animation: ri-rise calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease-out both;\n}\n\n@keyframes ri-rise {\n  from {\n    opacity: 0;\n    transform: translateY(8px);\n  }\n  to {\n    opacity: 1;\n    transform: translateY(0);\n  }\n}\n\n.ri-carousel {\n  display: grid;\n  gap: var(--space-4, 16px);\n}\n\n.ri-carousel-viewport {\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n}\n\n.ri-carousel-track {\n  display: flex;\n  transform: translateX(0);\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)) ease;\n  touch-action: pan-y;\n}\n\n.ri-slide,\n[data-ri-slide] {\n  flex: 0 0 100%;\n  min-width: 0;\n  min-height: 220px;\n  margin: 0;\n  padding: var(--space-8, 32px);\n}\n\n.ri-quote {\n  margin: 0;\n  font-size: 24px;\n  line-height: 32px;\n  letter-spacing: 0;\n}\n\n.ri-cite {\n  display: block;\n  margin-top: var(--space-6, 24px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-style: normal;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-carousel-controls {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--space-2, 8px);\n  align-items: center;\n  justify-content: space-between;\n}\n\n.ri-dot-row {\n  display: flex;\n  gap: var(--space-1, 4px);\n}\n\n.ri-dot {\n  width: var(--touch-target-min, 44px);\n  min-height: var(--touch-target-min, 44px);\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n}\n\n.ri-dot::before {\n  content: \"\";\n  display: block;\n  width: 8px;\n  height: 8px;\n  margin: auto;\n  border-radius: 999px;\n  background: var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-dot[aria-selected=\"true\"]::before {\n  width: 22px;\n  background: var(--color-text, #000);\n}\n\n.ri-form-grid {\n  display: grid;\n  grid-template-columns: minmax(0, 0.8fr) minmax(260px, 1.2fr);\n  gap: var(--space-8, 32px);\n}\n\n.ri-form-aside {\n  display: grid;\n  align-content: start;\n  gap: var(--space-6, 24px);\n}\n\n.ri-line-map {\n  position: relative;\n  min-height: 220px;\n  overflow: hidden;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background:\n    linear-gradient(90deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    linear-gradient(0deg, transparent 23px, var(--color-border, rgba(0, 0, 0, 0.1)) 24px, transparent 25px),\n    var(--color-surface, #f2f2f2);\n  background-size: 48px 48px;\n}\n\n.ri-line-map::before,\n.ri-line-map::after {\n  content: \"\";\n  position: absolute;\n  border: var(--border-width, 1px) solid var(--color-text, #000);\n  border-radius: 999px;\n}\n\n.ri-line-map::before {\n  inset: 34px auto auto 34px;\n  width: 116px;\n  height: 116px;\n}\n\n.ri-line-map::after {\n  right: 28px;\n  bottom: 30px;\n  width: 78px;\n  height: 78px;\n}\n\n.ri-map-path {\n  position: absolute;\n  left: 88px;\n  top: 88px;\n  width: 52%;\n  height: 1px;\n  background: var(--color-text, #000);\n  transform: rotate(18deg);\n  transform-origin: left center;\n}\n\n.ri-field-grid {\n  display: grid;\n  grid-template-columns: 1fr 1fr;\n  gap: var(--space-4, 16px);\n}\n\n.ri-field {\n  display: grid;\n  gap: var(--space-1, 4px);\n}\n\n.ri-field-full {\n  grid-column: 1 / -1;\n}\n\n.ri-label {\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-input,\n.ri-select {\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  border-radius: 0;\n  padding: 10px 12px;\n  background: var(--color-bg, #fff);\n  color: var(--color-text, #000);\n}\n\n.ri-input[aria-invalid=\"true\"] {\n  border-color: #c82124;\n}\n\n.ri-error {\n  min-height: 20px;\n  color: #c82124;\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status {\n  min-height: 24px;\n  margin: var(--space-3, 12px) 0 0;\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n  font-size: var(--font-size-label, 13px);\n  line-height: var(--line-height-label, 20px);\n}\n\n.ri-status[data-state=\"success\"] {\n  color: #0b6b2b;\n}\n\n.ri-status[data-state=\"error\"] {\n  color: #c82124;\n}\n\n.ri-faq-list {\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-item {\n  border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-faq-button {\n  display: flex;\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--space-4, 16px);\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: var(--space-4, 16px) 0;\n  text-align: left;\n}\n\n.ri-plus {\n  flex: 0 0 auto;\n  font-size: 22px;\n  line-height: 1;\n  transition: transform calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-button[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-faq-panel {\n  height: 0;\n  overflow: hidden;\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-faq-panel[hidden],\n.ri-nav-sublist[hidden] {\n  display: none;\n}\n\n.ri-faq-panel-inner {\n  padding: 0 0 var(--space-4, 16px);\n  color: var(--color-muted, rgba(0, 0, 0, 0.6));\n}\n\n.ri-theme-preview {\n  margin-top: var(--space-6, 24px);\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-6, 24px);\n  transition: background-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1)), border-color calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-theme-swatch-row {\n  display: grid;\n  grid-template-columns: repeat(4, minmax(0, 1fr));\n  gap: var(--space-2, 8px);\n  margin-top: var(--space-4, 16px);\n}\n\n.ri-theme-swatch {\n  min-height: 72px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-demo {\n  display: grid;\n  grid-template-columns: 260px minmax(0, 1fr);\n  min-height: 420px;\n  border: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n}\n\n.ri-nav-panel {\n  position: relative;\n  border-right: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  background: var(--color-surface, #f2f2f2);\n  padding: var(--space-4, 16px);\n}\n\n.ri-nav-toggle {\n  display: none;\n}\n\n.ri-nav-list,\n.ri-nav-sublist {\n  display: grid;\n  gap: var(--space-1, 4px);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.ri-nav-link,\n.ri-nav-category {\n  display: flex;\n  min-height: var(--touch-target-min, 44px);\n  width: 100%;\n  align-items: center;\n  justify-content: space-between;\n  border: 0;\n  background: transparent;\n  color: var(--color-text, #000);\n  padding: 10px 12px;\n  text-align: left;\n}\n\n.ri-nav-link {\n  text-decoration: none;\n}\n\n.ri-nav-link[aria-current=\"page\"] {\n  background: var(--color-bg, #fff);\n}\n\n.ri-nav-indicator {\n  width: 24px;\n  height: 2px;\n  background: currentColor;\n  opacity: 0;\n  transform: scaleX(0.4);\n  transform-origin: right center;\n  transition: opacity calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1)), transform calc(var(--duration-fast, 150ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-link[aria-current=\"page\"] .ri-nav-indicator {\n  opacity: 1;\n  transform: scaleX(1);\n}\n\n.ri-nav-sublist {\n  height: 0;\n  overflow: hidden;\n  padding-left: var(--space-4, 16px);\n  transition: height calc(var(--duration-standard, 200ms) * var(--ri-speed-factor, 1));\n}\n\n.ri-nav-category[aria-expanded=\"true\"] .ri-plus {\n  transform: rotate(45deg);\n}\n\n.ri-nav-content {\n  padding: var(--space-6, 24px);\n}\n\n.ri-nav-preview {\n  display: grid;\n  gap: var(--space-6, 24px);\n  max-height: 360px;\n  overflow: auto;\n  scroll-behavior: smooth;\n  scroll-padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section {\n  min-height: 260px;\n  border-top: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  padding-top: var(--space-4, 16px);\n}\n\n.ri-nav-section:first-child {\n  border-top: 0;\n  padding-top: 0;\n}\n\n@media (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@container ri-sample (max-width: 760px) {\n  .ri-shell {\n    padding: var(--space-4, 16px);\n  }\n\n  .ri-tabs-list,\n  .ri-sample[data-ri=\"tabs\"] [role=\"tablist\"] {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-tab-panel,\n  .ri-form-grid,\n  .ri-field-grid,\n  .ri-nav-demo {\n    grid-template-columns: 1fr;\n  }\n\n  .ri-panel-title,\n  .ri-quote {\n    font-size: 22px;\n    line-height: 28px;\n  }\n\n  .ri-carousel-controls {\n    justify-content: flex-start;\n  }\n\n  .ri-theme-swatch-row {\n    grid-template-columns: 1fr 1fr;\n  }\n\n  .ri-nav-demo {\n    min-height: 360px;\n  }\n\n  .ri-nav-panel {\n    border-right: 0;\n    border-bottom: var(--border-width, 1px) solid var(--color-border, rgba(0, 0, 0, 0.1));\n  }\n\n  .ri-nav-toggle {\n    display: inline-flex;\n    margin-bottom: var(--space-3, 12px);\n  }\n\n  .ri-nav-menu {\n    display: none;\n  }\n\n  .ri-nav-menu[data-open=\"true\"] {\n    display: block;\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ri-sample,\n  .ri-sample *,\n  .ri-sample *::before,\n  .ri-sample *::after {\n    animation: none !important;\n    scroll-behavior: auto !important;\n    transition-duration: 0ms !important;\n  }\n}\n\nhtml[data-ri-paused] .ri-sample,\nhtml[data-ri-paused] .ri-sample *,\nhtml[data-ri-paused] .ri-sample *::before,\nhtml[data-ri-paused] .ri-sample *::after {\n  animation-play-state: paused !important;\n  transition-duration: 0ms !important;\n}\n\n\n*{box-sizing:border-box}body{margin:0;padding:32px 24px;color:var(--color-text);background:var(--color-bg);font:14px/1.6 var(--font-body)}main{max-width:1100px;margin:auto}h1{font-size:28px;font-weight:500;letter-spacing:-.03em}header>p{color:var(--color-muted);font-size:12px;max-width:80ch}.standalone-controls{display:flex;gap:8px;align-items:center;flex-wrap:wrap;padding:16px 0;margin:24px 0;border-block:1px solid var(--color-border)}.standalone-controls button,.standalone-controls select{font:inherit;min-height:44px;padding:8px 16px;color:inherit;background:var(--color-bg);border:1px solid var(--color-border);border-radius:999px;cursor:pointer}.standalone-stage{min-width:0;padding:24px 0}.standalone-note{margin-top:32px;color:var(--color-muted);font-size:11px} :focus-visible{outline:2px solid var(--color-focus);outline-offset:3px}@media(max-width:600px){body{padding:20px 16px}.standalone-stage{padding:12px 0}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}\n</style></head><body><main><header><p>REFERENCE SYSTEM / INDEPENDENT EXAMPLE</p><h1>고정 탐색 · 스크롤 연동</h1><p>좌측 메뉴와 콘텐츠 스크롤을 연결합니다. 모바일 메뉴 열기·닫기, Escape, 접힌 하위 항목을 확인하세요.</p><p>본문을 스크롤하는 동안 좌측 탐색이 유지됩니다. 하위 메뉴·모바일 패널·스크롤 활성 추적은 실제 적용을 위한 보완입니다.</p></header><div class=\"standalone-controls\"><button id=\"replay\">다시 재생 ↻</button><button id=\"pause\" aria-pressed=\"false\">일시정지</button><label for=\"speed\">속도</label><select id=\"speed\"><option value=\"0.5\">0.5×</option><option value=\"1\" selected>1×</option><option value=\"2\">2×</option></select><button id=\"theme\" aria-pressed=\"false\">다크 테마</button></div><div id=\"example\" class=\"standalone-stage\"><section class=\"ri-sample\" data-ri=\"navigation\" aria-labelledby=\"ri-nav-demo-title\"><div class=\"ri-shell\"><h3 class=\"ri-heading\" id=\"ri-nav-demo-title\">반응형 사이드바 내비게이션</h3><p class=\"ri-copy\">좌측 탐색은 영상 첫 화면에서 관찰됩니다. 모바일에서는 버튼으로 메뉴를 열고 Escape로 닫으며 포커스를 트리거로 되돌립니다.</p><div class=\"ri-nav-demo\" style=\"margin-top:24px\"><aside class=\"ri-nav-panel\" aria-label=\"샘플 내비게이션\"><button class=\"ri-button ri-nav-toggle\" type=\"button\" data-ri-nav-toggle aria-expanded=\"false\" aria-controls=\"ri-nav-menu\">메뉴</button><nav class=\"ri-nav-menu\" id=\"ri-nav-menu\" data-ri-nav-menu data-open=\"false\"><ul class=\"ri-nav-list\"><li><a class=\"ri-nav-link\" href=\"#overview\" data-ri-nav-link data-title=\"Overview\" data-copy=\"광고 페이지의 메시지와 주요 CTA를 먼저 확인합니다.\" aria-current=\"page\">Overview <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><button class=\"ri-nav-category\" type=\"button\" data-ri-nav-category aria-expanded=\"true\" aria-controls=\"ri-nav-campaigns\">Campaigns <span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><ul class=\"ri-nav-sublist\" id=\"ri-nav-campaigns\"><li><a class=\"ri-nav-link\" href=\"#goals\" data-ri-nav-link data-title=\"Goals\" data-copy=\"목표 기반 캠페인 단계를 선택합니다.\">Goals <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><a class=\"ri-nav-link\" href=\"#formats\" data-ri-nav-link data-title=\"Formats\" data-copy=\"광고 형식과 예시 카드를 검토합니다.\">Formats <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li></ul></li><li><button class=\"ri-nav-category\" type=\"button\" data-ri-nav-category aria-expanded=\"false\" aria-controls=\"ri-nav-support\">Support <span class=\"ri-plus\" aria-hidden=\"true\">+</span></button><ul class=\"ri-nav-sublist\" id=\"ri-nav-support\"><li><a class=\"ri-nav-link\" href=\"#contact\" data-ri-nav-link data-title=\"Contact\" data-copy=\"전문가 상담 요청 폼으로 이동합니다.\">Contact <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li><li><a class=\"ri-nav-link\" href=\"#faq\" data-ri-nav-link data-title=\"FAQ\" data-copy=\"자주 묻는 질문을 펼쳐 확인합니다.\">FAQ <span class=\"ri-nav-indicator\" aria-hidden=\"true\"></span></a></li></ul></li></ul></nav></aside><div class=\"ri-nav-content\"><p class=\"ri-panel-kicker\">Active section</p><h4 class=\"ri-panel-title\" data-ri-nav-title>Overview</h4><p class=\"ri-panel-copy\" data-ri-nav-copy>광고 페이지의 메시지와 주요 CTA를 먼저 확인합니다.</p><div class=\"ri-nav-preview\" data-ri-nav-preview tabindex=\"0\" aria-label=\"섹션 스크롤 미리보기\"><section class=\"ri-nav-section\" id=\"overview\" data-ri-nav-section><p class=\"ri-panel-kicker\">Overview</p><h5 class=\"ri-panel-title\">큰 메시지와 CTA</h5><p class=\"ri-panel-copy\">첫 화면의 핵심 문장, 보조 설명, 행동 버튼을 같은 위계로 유지합니다.</p></section><section class=\"ri-nav-section\" id=\"goals\" data-ri-nav-section><p class=\"ri-panel-kicker\">Goals</p><h5 class=\"ri-panel-title\">목표 기반 단계</h5><p class=\"ri-panel-copy\">탭과 단계 설명으로 캠페인 설정 흐름을 좁고 선명하게 보여줍니다.</p></section><section class=\"ri-nav-section\" id=\"formats\" data-ri-nav-section><p class=\"ri-panel-kicker\">Formats</p><h5 class=\"ri-panel-title\">광고 형식 카드</h5><p class=\"ri-panel-copy\">형식별 미리보기와 설명을 스크롤 가능한 본문에서 비교합니다.</p></section><section class=\"ri-nav-section\" id=\"contact\" data-ri-nav-section><p class=\"ri-panel-kicker\">Contact</p><h5 class=\"ri-panel-title\">상담 요청</h5><p class=\"ri-panel-copy\">필수 입력, 지연, 성공, 실패 상태를 한 흐름에서 처리합니다.</p></section><section class=\"ri-nav-section\" id=\"faq\" data-ri-nav-section><p class=\"ri-panel-kicker\">FAQ</p><h5 class=\"ri-panel-title\">자주 묻는 질문</h5><p class=\"ri-panel-copy\">긴 페이지 끝에서 질문을 펼치고 닫는 흐름을 제공합니다.</p></section></div></div></div></div></section></div><p id=\"state\" class=\"standalone-note\" role=\"status\">외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.</p></main>\n<script>(function () {\n  const mountedRoots = new WeakMap();\n  const mountedSamples = new WeakMap();\n  const preparedSamples = new WeakSet();\n  let uid = 0;\n  const apiState = {\n    paused: false,\n    speed: 1,\n    reduced: false,\n    reduceQuery: null,\n    carouselRefreshers: new Set()\n  };\n\n  const clampSpeed = (value) => {\n    const next = Number(value);\n    if (!Number.isFinite(next) || next <= 0) return 1;\n    return Math.min(4, Math.max(0.25, next));\n  };\n\n  const setSpeedVar = () => {\n    document.documentElement.style.setProperty('--ri-speed-factor', String(1 / apiState.speed));\n  };\n\n  const getRoot = (root) => root || document;\n  const queryAll = (root, selector) => Array.from(getRoot(root).querySelectorAll(selector));\n  const focusableSelector = 'a, button, input, select, textarea, [tabindex]';\n  const samplesIn = (root) => {\n    const resolvedRoot = getRoot(root);\n    const samples = queryAll(resolvedRoot, '[data-ri]');\n    if (resolvedRoot.nodeType === 1 && resolvedRoot.matches('[data-ri]')) samples.unshift(resolvedRoot);\n    return samples;\n  };\n  const getScopedId = (scope, id) => {\n    if (!id) return null;\n    if (scope.nodeType === 1 && scope.id === id) return scope;\n    return queryAll(scope, '[id]').find((node) => node.id === id) || null;\n  };\n  const setButtonLabel = (button, text, leadingNode, trailingNode) => {\n    button.textContent = '';\n    if (leadingNode) {\n      button.appendChild(leadingNode);\n      button.appendChild(document.createTextNode(' '));\n    }\n    button.appendChild(document.createTextNode(text));\n    if (trailingNode) {\n      button.appendChild(document.createTextNode(' '));\n      button.appendChild(trailingNode);\n    }\n  };\n\n  const listen = (target, type, handler, options, cleanup) => {\n    if (!target) return;\n    target.addEventListener(type, handler, options);\n    cleanup.push(() => target.removeEventListener(type, handler, options));\n  };\n\n  const clearTimer = (state, key) => {\n    if (state[key]) {\n      window.clearTimeout(state[key]);\n      state[key] = 0;\n    }\n  };\n\n  const isReduced = () => Boolean(apiState.reduced || (apiState.reduceQuery && apiState.reduceQuery.matches));\n  const duration = (ms) => isReduced() ? 0 : Math.max(0, ms / apiState.speed);\n\n  const remapIds = (sample) => {\n    if (preparedSamples.has(sample)) return;\n    const map = new Map();\n    queryAll(sample, '[id]').forEach((node) => {\n      const next = `${node.id}--ri-${++uid}`;\n      map.set(node.id, next);\n      node.id = next;\n    });\n    const remapTokenList = (value) => value.split(/\\s+/).map((token) => map.get(token) || token).join(' ');\n    const scopedNodes = (selector) => {\n      const nodes = queryAll(sample, selector);\n      if (sample.matches(selector)) nodes.unshift(sample);\n      return nodes;\n    };\n    ['aria-controls', 'aria-labelledby', 'aria-describedby', 'aria-owns'].forEach((attr) => {\n      scopedNodes(`[${attr}]`).forEach((node) => node.setAttribute(attr, remapTokenList(node.getAttribute(attr) || '')));\n    });\n    scopedNodes('label[for]').forEach((label) => {\n      const next = map.get(label.getAttribute('for'));\n      if (next) label.setAttribute('for', next);\n    });\n    queryAll(sample, 'a[href^=\"#\"]').forEach((link) => {\n      const next = map.get(link.getAttribute('href').slice(1));\n      if (next) link.setAttribute('href', `#${next}`);\n    });\n    preparedSamples.add(sample);\n    sample.dataset.riIdsReady = 'true';\n  };\n\n  const setContainedFocusAccess = (container, open) => {\n    container.toggleAttribute('inert', !open);\n    queryAll(container, focusableSelector).forEach((item) => {\n      if (open) {\n        if (item.dataset.riSavedTabIndex === '') item.removeAttribute('tabindex');\n        else if (item.dataset.riSavedTabIndex !== undefined) item.setAttribute('tabindex', item.dataset.riSavedTabIndex);\n        delete item.dataset.riSavedTabIndex;\n      } else {\n        if (item.dataset.riSavedTabIndex === undefined) item.dataset.riSavedTabIndex = item.getAttribute('tabindex') || '';\n        item.setAttribute('tabindex', '-1');\n      }\n    });\n  };\n\n  const setPanelAccess = (panel, open) => {\n    if (!panel) return;\n    if (!open && panel.contains(document.activeElement) && panel.previousElementSibling && panel.previousElementSibling.focus) {\n      panel.previousElementSibling.focus();\n    }\n    panel.hidden = false;\n    setContainedFocusAccess(panel, open);\n    panel.setAttribute('aria-hidden', String(!open));\n  };\n\n  const clearPanelTimer = (panel) => {\n    if (panel && panel.dataset.riHeightTimer) window.clearTimeout(Number(panel.dataset.riHeightTimer));\n    if (panel) delete panel.dataset.riHeightTimer;\n  };\n\n  const finishPanelClose = (panel) => {\n    if (panel && panel.dataset.riOpen !== 'true') panel.hidden = true;\n  };\n\n  const animateHeight = (panel, open) => {\n    if (!panel) return;\n    clearPanelTimer(panel);\n    setPanelAccess(panel, open);\n    if (isReduced()) {\n      panel.style.height = open ? 'auto' : '0px';\n      finishPanelClose(panel);\n      return;\n    }\n    const start = panel.getBoundingClientRect().height;\n    panel.style.height = `${start}px`;\n    panel.offsetHeight;\n    panel.style.height = open ? `${panel.scrollHeight}px` : '0px';\n    if (open) {\n      const done = () => {\n        if (panel.dataset.riOpen === 'true') panel.style.height = 'auto';\n      };\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        done();\n      }, duration(220)));\n    } else {\n      panel.dataset.riHeightTimer = String(window.setTimeout(() => {\n        delete panel.dataset.riHeightTimer;\n        finishPanelClose(panel);\n      }, duration(220)));\n    }\n  };\n\n  const mountButtons = (sample, cleanup, options = {}) => {\n    const action = sample.querySelector('[data-ri-button-action]');\n    const status = sample.querySelector('[data-ri-button-status]');\n    const state = { timer: 0, pending: false, actionId: 0, controller: null };\n    const idleHtml = action ? action.innerHTML : '';\n    const idleText = status ? status.textContent : '';\n    const abortPending = () => {\n      state.actionId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => {\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n    });\n\n    const reset = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      if (action) {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n      }\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = idleText || '버튼 상태 피드백은 권장 추가 항목입니다. 외부 요청 없이 로컬에서만 실행됩니다.';\n      }\n    };\n\n    listen(action, 'click', async () => {\n      const production = sample.dataset.riMode === 'production';\n      if (production && typeof options.onAction !== 'function') {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        action.innerHTML = idleHtml;\n        if (status) {\n          status.dataset.state = 'error';\n          status.textContent = '요청 기능이 연결되지 않았습니다.';\n        }\n        return;\n      }\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      action.disabled = true;\n      action.setAttribute('aria-busy', 'true');\n      const spinner = document.createElement('span');\n      spinner.className = 'ri-spinner';\n      spinner.setAttribute('aria-hidden', 'true');\n      setButtonLabel(action, action.dataset.riLoadingText || '확인 중', spinner);\n      if (status) {\n        status.dataset.state = '';\n        status.textContent = action.dataset.riLoadingMessage || '로컬 확인 상태를 표시하는 중입니다.';\n      }\n      if (typeof options.onAction === 'function') {\n        const actionId = state.actionId + 1;\n        state.actionId = actionId;\n        state.pending = true;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        try {\n          const result = await options.onAction({ button: action, signal: state.controller.signal });\n          if (state.actionId !== actionId || state.controller.signal.aborted) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'success';\n            status.textContent = result && result.message ? String(result.message) : '요청이 완료되었습니다.';\n          }\n        } catch (error) {\n          if (state.actionId !== actionId || (state.controller && state.controller.signal.aborted)) return;\n          action.disabled = false;\n          action.setAttribute('aria-busy', 'false');\n          action.innerHTML = idleHtml;\n          state.pending = false;\n          state.controller = null;\n          if (status) {\n            status.dataset.state = 'error';\n            status.textContent = '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.';\n          }\n        }\n        return;\n      }\n      state.timer = window.setTimeout(() => {\n        action.disabled = false;\n        action.setAttribute('aria-busy', 'false');\n        const arrow = document.createElement('span');\n        arrow.className = 'ri-button-arrow';\n        arrow.setAttribute('aria-hidden', 'true');\n        arrow.textContent = '->';\n        setButtonLabel(action, action.dataset.riSuccessText || '확인 완료', null, arrow);\n        if (status) {\n          status.dataset.state = 'success';\n          status.textContent = action.dataset.riSuccessMessage || '완료 상태가 같은 위치에 남아 레이아웃이 흔들리지 않습니다.';\n        }\n      }, duration(800));\n    }, undefined, cleanup);\n\n    reset();\n    return reset;\n  };\n\n  const mountTabs = (sample, cleanup) => {\n    const tabs = queryAll(sample, '[role=\"tab\"]');\n    const panel = sample.querySelector('[role=\"tabpanel\"]');\n    const indicator = sample.querySelector('[data-ri-tabs-indicator]');\n    const list = sample.querySelector('[role=\"tablist\"]');\n    const title = sample.querySelector('[data-ri-panel-title]');\n    const copy = sample.querySelector('[data-ri-panel-copy]');\n    const kicker = sample.querySelector('[data-ri-panel-kicker]');\n    const state = { index: 0, timer: 0 };\n    cleanup.push(() => clearTimer(state, 'timer'));\n\n    const moveIndicator = (animated = true) => {\n      const active = tabs[state.index];\n      if (!active || !indicator || !list) return;\n      const activeBox = active.getBoundingClientRect();\n      const listBox = list.getBoundingClientRect();\n      const rowOffset = activeBox.bottom - listBox.bottom;\n      indicator.toggleAttribute('data-ri-no-transition', !animated);\n      indicator.style.width = `${activeBox.width}px`;\n      indicator.style.transform = `translate(${activeBox.left - listBox.left}px, ${rowOffset}px)`;\n      if (!animated) {\n        requestAnimationFrame(() => {\n          indicator.removeAttribute('data-ri-no-transition');\n        });\n      }\n    };\n\n    const select = (index, focus) => {\n      state.index = (index + tabs.length) % tabs.length;\n      tabs.forEach((tab, tabIndex) => {\n        const active = tabIndex === state.index;\n        tab.setAttribute('aria-selected', String(active));\n        tab.tabIndex = active ? 0 : -1;\n      });\n      const active = tabs[state.index];\n      if (panel && active) {\n        panel.setAttribute('aria-labelledby', active.id);\n        panel.classList.remove('ri-entering');\n        panel.offsetHeight;\n        if (!isReduced()) panel.classList.add('ri-entering');\n        clearTimer(state, 'timer');\n        state.timer = window.setTimeout(() => panel.classList.remove('ri-entering'), duration(240));\n      }\n      if (kicker) kicker.textContent = active.dataset.kicker || '';\n      if (title) title.textContent = active.dataset.title || active.textContent.trim();\n      if (copy) copy.textContent = active.dataset.copy || '';\n      moveIndicator(true);\n      if (focus && active) active.focus();\n    };\n\n    tabs.forEach((tab, index) => {\n      listen(tab, 'click', () => select(index, false), undefined, cleanup);\n      listen(tab, 'keydown', (event) => {\n        const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n        if (event.key in keys) {\n          event.preventDefault();\n          select(state.index + keys[event.key], true);\n        } else if (event.key === 'Home') {\n          event.preventDefault();\n          select(0, true);\n        } else if (event.key === 'End') {\n          event.preventDefault();\n          select(tabs.length - 1, true);\n        }\n      }, undefined, cleanup);\n    });\n    listen(window, 'resize', () => moveIndicator(false), undefined, cleanup);\n    if ('ResizeObserver' in window && list) {\n      const observer = new ResizeObserver(() => moveIndicator(false));\n      observer.observe(list);\n      tabs.forEach((tab) => observer.observe(tab));\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const reset = () => select(0, false);\n    reset();\n    return reset;\n  };\n\n  const mountCarousel = (sample, cleanup) => {\n    const track = sample.querySelector('[data-ri-carousel-track]');\n    const slides = queryAll(sample, '[data-ri-slide]');\n    const dots = queryAll(sample, '[data-ri-dot]');\n    const prev = sample.querySelector('[data-ri-prev]');\n    const next = sample.querySelector('[data-ri-next]');\n    const toggle = sample.querySelector('[data-ri-autoplay]');\n    const status = sample.querySelector('[data-ri-carousel-status]');\n    const viewport = sample.querySelector('[data-ri-carousel-viewport]');\n    const state = { index: 0, autoplay: false, hovered: false, focused: false, offscreen: false, timer: 0, touchX: 0 };\n\n    const stopTimer = () => clearTimer(state, 'timer');\n    const canRun = () => state.autoplay && !state.hovered && !state.focused && !state.offscreen && !apiState.paused && !isReduced() && !document.hidden;\n    const schedule = () => {\n      stopTimer();\n      if (!canRun()) return;\n      state.timer = window.setTimeout(() => {\n        go(state.index + 1);\n        schedule();\n      }, duration(4200));\n    };\n    const updateAutoplay = () => {\n      if (toggle) {\n        toggle.setAttribute('aria-pressed', String(state.autoplay));\n        toggle.textContent = state.autoplay ? '자동 재생 끄기' : '자동 재생 켜기';\n      }\n      schedule();\n    };\n    const go = (index) => {\n      state.index = (index + slides.length) % slides.length;\n      if (track) track.style.transform = `translateX(${-100 * state.index}%)`;\n      slides.forEach((slide, slideIndex) => {\n        const active = slideIndex === state.index;\n        if (!active && slide.contains(document.activeElement)) {\n          const fallback = dots[state.index] || next || prev;\n          if (fallback && fallback.focus) fallback.focus();\n        }\n        slide.setAttribute('aria-hidden', String(!active));\n        setContainedFocusAccess(slide, active);\n      });\n      dots.forEach((dot, dotIndex) => {\n        const active = dotIndex === state.index;\n        dot.setAttribute('aria-selected', String(active));\n        dot.tabIndex = active ? 0 : -1;\n      });\n      if (status) status.textContent = `${state.index + 1} / ${slides.length}`;\n    };\n    const setPauseFlag = (key, value) => {\n      state[key] = value;\n      schedule();\n    };\n\n    listen(prev, 'click', () => { go(state.index - 1); schedule(); }, undefined, cleanup);\n    listen(next, 'click', () => { go(state.index + 1); schedule(); }, undefined, cleanup);\n    dots.forEach((dot, index) => listen(dot, 'click', () => { go(index); schedule(); }, undefined, cleanup));\n    dots.forEach((dot) => listen(dot, 'keydown', (event) => {\n      const keys = { ArrowRight: 1, ArrowDown: 1, ArrowLeft: -1, ArrowUp: -1 };\n      if (event.key in keys) {\n        event.preventDefault();\n        go(state.index + keys[event.key]);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'Home') {\n        event.preventDefault();\n        go(0);\n        dots[state.index].focus();\n        schedule();\n      } else if (event.key === 'End') {\n        event.preventDefault();\n        go(dots.length - 1);\n        dots[state.index].focus();\n        schedule();\n      }\n    }, undefined, cleanup));\n    listen(toggle, 'click', () => { state.autoplay = !state.autoplay; updateAutoplay(); }, undefined, cleanup);\n    listen(sample, 'mouseenter', () => setPauseFlag('hovered', true), undefined, cleanup);\n    listen(sample, 'mouseleave', () => setPauseFlag('hovered', false), undefined, cleanup);\n    listen(sample, 'focusin', () => setPauseFlag('focused', true), undefined, cleanup);\n    listen(sample, 'focusout', (event) => {\n      if (!sample.contains(event.relatedTarget)) setPauseFlag('focused', false);\n    }, undefined, cleanup);\n    listen(document, 'visibilitychange', schedule, undefined, cleanup);\n    listen(viewport, 'touchstart', (event) => {\n      state.touchX = event.changedTouches[0].clientX;\n    }, { passive: true }, cleanup);\n    listen(viewport, 'touchend', (event) => {\n      const delta = event.changedTouches[0].clientX - state.touchX;\n      if (Math.abs(delta) > 36) {\n        go(state.index + (delta < 0 ? 1 : -1));\n        schedule();\n      }\n    }, { passive: true }, cleanup);\n    if ('IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        state.offscreen = entries.some((entry) => !entry.isIntersecting);\n        schedule();\n      }, { threshold: 0.2 });\n      observer.observe(sample);\n      cleanup.push(() => observer.disconnect());\n    }\n\n    const refresh = () => schedule();\n    apiState.carouselRefreshers.add(refresh);\n    cleanup.push(() => {\n      stopTimer();\n      apiState.carouselRefreshers.delete(refresh);\n    });\n\n    const reset = () => {\n      stopTimer();\n      state.index = 0;\n      state.autoplay = false;\n      state.hovered = false;\n      state.focused = false;\n      state.offscreen = false;\n      go(0);\n      updateAutoplay();\n    };\n    reset();\n    return reset;\n  };\n\n  const mountForm = (sample, cleanup, options = {}) => {\n    const form = sample.querySelector('form');\n    const status = sample.querySelector('[data-ri-form-status]');\n    const selector = sample.querySelector('[data-ri-form-mode]');\n    const submit = sample.querySelector('button[type=\"submit\"]');\n    const controlled = sample.dataset.riMode === 'production' || typeof options.onSubmit === 'function';\n    const idleStatus = status ? status.textContent : '';\n    const state = { timer: 0, resetTimer: 0, internalReset: false, pending: false, submitId: 0, controller: null };\n    const required = queryAll(sample, '[data-ri-required]');\n\n    const abortPending = () => {\n      state.submitId += 1;\n      state.pending = false;\n      if (state.controller) state.controller.abort();\n      state.controller = null;\n    };\n    cleanup.push(() => clearTimer(state, 'timer'));\n    cleanup.push(() => clearTimer(state, 'resetTimer'));\n    cleanup.push(() => {\n      abortPending();\n      setPending(false);\n    });\n\n    const setError = (input, message) => {\n      const ids = (input.getAttribute('aria-describedby') || '').split(/\\s+/).filter(Boolean);\n      const nodes = ids.map((id) => getScopedId(sample, id)).filter(Boolean);\n      const error = nodes.find((node) => node.matches('.ri-error, [data-ri-error]')) || nodes[nodes.length - 1];\n      input.setAttribute('aria-invalid', message ? 'true' : 'false');\n      if (error) error.textContent = message || '';\n    };\n    const validate = () => {\n      let valid = true;\n      required.forEach((input) => {\n        let message = '';\n        if (!input.value.trim()) message = '필수 입력 항목입니다.';\n        if (!message && input.type === 'email' && !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(input.value)) message = '올바른 이메일 주소를 입력하세요.';\n        setError(input, message);\n        if (message) valid = false;\n      });\n      return valid;\n    };\n    const setStatus = (stateName, text) => {\n      if (!status) return;\n      status.dataset.state = stateName;\n      status.textContent = text;\n    };\n    const setPending = (pending) => {\n      state.pending = pending;\n      if (!submit) return;\n      submit.disabled = pending;\n      submit.setAttribute('aria-busy', String(pending));\n    };\n    const resetVisual = () => {\n      clearTimer(state, 'timer');\n      abortPending();\n      setPending(false);\n      required.forEach((input) => setError(input, ''));\n      setStatus('', controlled ? idleStatus : '제출 결과는 데모 선택값으로 재현합니다. 네트워크 요청은 발생하지 않습니다.');\n    };\n    const reset = () => {\n      if (form) {\n        state.internalReset = true;\n        form.reset();\n        state.internalReset = false;\n      }\n      resetVisual();\n    };\n\n    listen(form, 'input', (event) => {\n      if (event.target && event.target.matches('[data-ri-required]')) setError(event.target, '');\n    }, undefined, cleanup);\n    listen(form, 'reset', () => {\n      if (state.internalReset) return;\n      clearTimer(state, 'resetTimer');\n      abortPending();\n      state.resetTimer = window.setTimeout(() => {\n        state.resetTimer = 0;\n        resetVisual();\n      }, 0);\n    }, undefined, cleanup);\n    listen(form, 'submit', async (event) => {\n      event.preventDefault();\n      if (state.pending) return;\n      clearTimer(state, 'timer');\n      if (!validate()) {\n        const firstInvalid = sample.querySelector('[aria-invalid=\"true\"]');\n        if (firstInvalid) firstInvalid.focus();\n        setStatus('error', '입력값을 확인하세요.');\n        return;\n      }\n      if (sample.dataset.riMode === 'production' && typeof options.onSubmit !== 'function') {\n        setPending(false);\n        setStatus('error', '전송 기능이 연결되지 않았습니다.');\n        return;\n      }\n      if (typeof options.onSubmit === 'function') {\n        const submitId = state.submitId + 1;\n        state.submitId = submitId;\n        if (state.controller) state.controller.abort();\n        state.controller = new AbortController();\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        try {\n          const values = Object.fromEntries(new FormData(form).entries());\n          const result = await options.onSubmit({ values, form, signal: state.controller.signal });\n          if (state.submitId !== submitId || state.controller.signal.aborted) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('success', result && result.message ? String(result.message) : '요청이 접수되었습니다.');\n        } catch (error) {\n          if (state.submitId !== submitId || (state.controller && state.controller.signal.aborted)) return;\n          setPending(false);\n          state.controller = null;\n          setStatus('error', '요청을 완료하지 못했습니다. 잠시 후 다시 시도하세요.');\n        }\n        return;\n      }\n      const mode = selector ? selector.value : 'success';\n      if (mode === 'pending') {\n        setPending(true);\n        setStatus('', '상담 요청을 확인하는 중입니다.');\n        state.timer = window.setTimeout(() => {\n          setPending(false);\n          setStatus('success', '데모 지연 후 성공 상태로 전환했습니다.');\n        }, duration(900));\n      } else if (mode === 'failure') {\n        setPending(false);\n        setStatus('error', '데모 실패 상태입니다. 같은 화면에서 다시 시도할 수 있습니다.');\n      } else {\n        setPending(false);\n        setStatus('success', '데모 성공 상태입니다. 외부 전송 없이 완료했습니다.');\n      }\n    }, undefined, cleanup);\n\n    required.forEach((input) => {\n      input.required = true;\n      input.setAttribute('aria-required', 'true');\n    });\n    if (controlled) resetVisual();\n    else reset();\n    return reset;\n  };\n\n  const mountFAQ = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-faq-button]');\n    const setOpen = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    buttons.forEach((button) => listen(button, 'click', () => {\n      setOpen(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    const reset = () => buttons.forEach((button, index) => setOpen(button, index === 0));\n    reset();\n    return reset;\n  };\n\n  const mountTheme = (sample, cleanup) => {\n    const buttons = queryAll(sample, '[data-ri-theme-choice]');\n    const status = sample.querySelector('[data-ri-theme-status]');\n    const storageKey = sample.dataset.riStorageKey || 'reference-interactions-theme';\n    const systemQuery = window.matchMedia ? window.matchMedia('(prefers-color-scheme: dark)') : null;\n    let choice = 'system';\n\n    const readStored = () => {\n      try {\n        const stored = window.localStorage && window.localStorage.getItem(storageKey);\n        if (stored === 'light' || stored === 'dark' || stored === 'system') choice = stored;\n      } catch (error) {\n        choice = 'system';\n      }\n    };\n    const writeStored = () => {\n      try {\n        if (window.localStorage) window.localStorage.setItem(storageKey, choice);\n      } catch (error) {\n        return;\n      }\n    };\n    const apply = () => {\n      const resolved = choice === 'system' ? (systemQuery && systemQuery.matches ? 'dark' : 'light') : choice;\n      sample.dataset.theme = resolved;\n      sample.dataset.riThemeChoice = choice;\n      buttons.forEach((button) => button.setAttribute('aria-pressed', String(button.dataset.riThemeChoice === choice)));\n      if (status) status.textContent = `현재 샘플 테마: ${choice === 'system' ? `system -> ${resolved}` : resolved}`;\n    };\n\n    buttons.forEach((button) => listen(button, 'click', () => {\n      choice = button.dataset.riThemeChoice || 'system';\n      writeStored();\n      apply();\n    }, undefined, cleanup));\n    if (systemQuery) {\n      const handler = () => {\n        if (choice === 'system') apply();\n      };\n      if (systemQuery.addEventListener) listen(systemQuery, 'change', handler, undefined, cleanup);\n      else if (systemQuery.addListener) {\n        systemQuery.addListener(handler);\n        cleanup.push(() => systemQuery.removeListener(handler));\n      }\n    }\n    const reset = () => {\n      choice = 'system';\n      writeStored();\n      apply();\n    };\n    readStored();\n    apply();\n    return reset;\n  };\n\n  const mountNavigation = (sample, cleanup) => {\n    const toggle = sample.querySelector('[data-ri-nav-toggle]');\n    const menu = sample.querySelector('[data-ri-nav-menu]');\n    const categoryButtons = queryAll(sample, '[data-ri-nav-category]');\n    const links = queryAll(sample, '[data-ri-nav-link]');\n    const contentTitle = sample.querySelector('[data-ri-nav-title]');\n    const contentCopy = sample.querySelector('[data-ri-nav-copy]');\n    const preview = sample.querySelector('[data-ri-nav-preview]');\n    const sections = queryAll(sample, '[data-ri-nav-section]');\n    let lastFocus = null;\n\n    const setMenu = (open) => {\n      if (!menu || !toggle) return;\n      menu.dataset.open = String(open);\n      toggle.setAttribute('aria-expanded', String(open));\n      if (!open && lastFocus) lastFocus.focus();\n    };\n    const setCategory = (button, open) => {\n      const panel = getScopedId(sample, button.getAttribute('aria-controls'));\n      button.setAttribute('aria-expanded', String(open));\n      if (panel) panel.dataset.riOpen = String(open);\n      animateHeight(panel, open);\n    };\n    const activate = (link, scroll = false) => {\n      links.forEach((item) => item.removeAttribute('aria-current'));\n      link.setAttribute('aria-current', 'page');\n      if (contentTitle) contentTitle.textContent = link.dataset.title || link.textContent.trim();\n      if (contentCopy) contentCopy.textContent = link.dataset.copy || '선택한 섹션의 요약이 이 자리에 표시됩니다.';\n      if (scroll && preview) {\n        const href = link.getAttribute('href') || '';\n        const target = href.startsWith('#') ? getScopedId(sample, href.slice(1)) : null;\n        if (target) preview.scrollTo({ top: target.offsetTop - preview.offsetTop, behavior: isReduced() || apiState.paused ? 'auto' : 'smooth' });\n      }\n      if (window.matchMedia && window.matchMedia('(max-width: 760px)').matches) setMenu(false);\n    };\n    const activateBySection = (section) => {\n      const link = links.find((item) => item.getAttribute('href') === `#${section.id}`);\n      if (link) activate(link, false);\n    };\n\n    listen(toggle, 'click', () => {\n      lastFocus = toggle;\n      setMenu(!(menu && menu.dataset.open === 'true'));\n    }, undefined, cleanup);\n    listen(sample, 'keydown', (event) => {\n      if (event.key === 'Escape' && menu && menu.dataset.open === 'true') {\n        event.preventDefault();\n        setMenu(false);\n      }\n    }, undefined, cleanup);\n    categoryButtons.forEach((button) => listen(button, 'click', () => {\n      setCategory(button, button.getAttribute('aria-expanded') !== 'true');\n    }, undefined, cleanup));\n    links.forEach((link) => listen(link, 'click', (event) => {\n      event.preventDefault();\n      activate(link, true);\n    }, undefined, cleanup));\n    if (preview && sections.length && 'IntersectionObserver' in window) {\n      const observer = new IntersectionObserver((entries) => {\n        const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];\n        if (visible) activateBySection(visible.target);\n      }, { root: preview, threshold: [0.35, 0.6, 0.9] });\n      sections.forEach((section) => observer.observe(section));\n      cleanup.push(() => observer.disconnect());\n    } else if (preview && sections.length) {\n      listen(preview, 'scroll', () => {\n        const current = sections.reduce((best, section) => {\n          const distance = Math.abs(section.offsetTop - preview.scrollTop);\n          return !best || distance < best.distance ? { section, distance } : best;\n        }, null);\n        if (current) activateBySection(current.section);\n      }, { passive: true }, cleanup);\n    }\n\n    const reset = () => {\n      setMenu(false);\n      categoryButtons.forEach((button, index) => setCategory(button, index === 0));\n      if (preview) preview.scrollTop = 0;\n      if (links[0]) activate(links[0], false);\n    };\n    reset();\n    return reset;\n  };\n\n  const mountSample = (sample, cleanup, options) => {\n    const current = mountedSamples.get(sample);\n    if (current) {\n      current.refs += 1;\n      cleanup.push(() => {\n        if (!mountedSamples.has(sample)) return;\n        current.refs -= 1;\n        if (current.refs <= 0) {\n          while (current.cleanup.length) current.cleanup.pop()();\n          mountedSamples.delete(sample);\n        }\n      });\n      return current.reset;\n    }\n    remapIds(sample);\n    const type = sample.dataset.ri;\n    const mounts = {\n      buttons: mountButtons,\n      tabs: mountTabs,\n      carousel: mountCarousel,\n      form: mountForm,\n      faq: mountFAQ,\n      theme: mountTheme,\n      navigation: mountNavigation\n    };\n    if (!mounts[type]) return null;\n    const sampleCleanup = [];\n    sampleCleanup.push(() => queryAll(sample, '[data-ri-height-timer]').forEach(clearPanelTimer));\n    const reset = mounts[type](sample, sampleCleanup, options);\n    const state = { reset, cleanup: sampleCleanup, refs: 1 };\n    mountedSamples.set(sample, state);\n    cleanup.push(() => {\n      if (!mountedSamples.has(sample)) return;\n      state.refs -= 1;\n      if (state.refs <= 0) {\n        while (sampleCleanup.length) sampleCleanup.pop()();\n        mountedSamples.delete(sample);\n      }\n    });\n    return reset;\n  };\n\n  const refreshGlobalState = () => {\n    setSpeedVar();\n    document.documentElement.toggleAttribute('data-ri-paused', apiState.paused);\n    apiState.carouselRefreshers.forEach((refresh) => refresh());\n  };\n\n  const mount = (root = document, options = {}) => {\n    const resolvedRoot = getRoot(root);\n    if (mountedRoots.has(resolvedRoot)) {\n      const rootState = mountedRoots.get(resolvedRoot);\n      samplesIn(resolvedRoot).forEach((sample) => {\n        if (rootState.samples.has(sample)) return;\n        mountSample(sample, rootState.cleanup, options);\n        rootState.samples.add(sample);\n      });\n      return rootState.clean;\n    }\n    const cleanup = [];\n    const rootState = { cleanup, samples: new WeakSet(), cleaned: false, clean: null };\n    samplesIn(resolvedRoot).forEach((sample) => {\n      mountSample(sample, cleanup, options);\n      rootState.samples.add(sample);\n    });\n    const clean = () => {\n      if (rootState.cleaned) return;\n      rootState.cleaned = true;\n      while (cleanup.length) cleanup.pop()();\n      mountedRoots.delete(resolvedRoot);\n    };\n    rootState.clean = clean;\n    mountedRoots.set(resolvedRoot, rootState);\n    refreshGlobalState();\n    return clean;\n  };\n\n  const replay = (root = document) => {\n    samplesIn(getRoot(root)).forEach((sample) => {\n      const state = mountedSamples.get(sample);\n      if (state && state.reset) state.reset();\n    });\n  };\n\n  const setPaused = (value) => {\n    apiState.paused = Boolean(value);\n    refreshGlobalState();\n  };\n\n  const setSpeed = (value) => {\n    apiState.speed = clampSpeed(value);\n    refreshGlobalState();\n  };\n\n  if (window.matchMedia) {\n    apiState.reduceQuery = window.matchMedia('(prefers-reduced-motion: reduce)');\n    const reduceHandler = () => {\n      apiState.reduced = apiState.reduceQuery.matches;\n      refreshGlobalState();\n    };\n    apiState.reduced = apiState.reduceQuery.matches;\n    if (apiState.reduceQuery.addEventListener) apiState.reduceQuery.addEventListener('change', reduceHandler);\n    else if (apiState.reduceQuery.addListener) apiState.reduceQuery.addListener(reduceHandler);\n  }\n\n  window.ReferenceInteractions = { mount, replay, setPaused, setSpeed };\n})();\n<\/script><script>\nconst api = window.ReferenceInteractions;\nconst example = document.querySelector('#example');\nconst cleanup = api.mount(example);\nlet paused = false;\nconst reduce = matchMedia('(prefers-reduced-motion: reduce)');\nfunction status() { document.querySelector('#state').textContent = reduce.matches ? '모션 감소 설정 적용: 이동을 줄이고 기능과 최종 상태를 유지합니다.' : paused ? '모션 일시정지. 입력과 조작은 계속 사용할 수 있습니다.' : '외부 파일이나 패키지 없이 동작합니다. 폼은 로컬 시뮬레이션입니다.'; }\nreduce.addEventListener('change', status); status();\ndocument.querySelector('#replay').addEventListener('click', () => api.replay(example));\ndocument.querySelector('#pause').addEventListener('click', e => { paused=!paused;api.setPaused(paused);e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'재생':'일시정지';status(); });\ndocument.querySelector('#speed').addEventListener('change', e => api.setSpeed(Number(e.target.value)));\ndocument.querySelector('#theme').addEventListener('click', e => {const dark=document.body.dataset.theme!=='dark';document.body.dataset.theme=dark?'dark':'light';e.currentTarget.setAttribute('aria-pressed',String(dark));e.currentTarget.textContent=dark?'라이트 테마':'다크 테마';});\naddEventListener('pagehide', e => {if(!e.persisted && typeof cleanup==='function')cleanup();});\n<\/script></body></html>"}};
