const {chromium}=require(process.env.PLAYWRIGHT_MODULE || '/home/kang/.claude/skills/gstack/node_modules/playwright');
const fs=require('fs');
(async()=>{const b=await chromium.launch({headless:true});const page=await b.newPage({viewport:{width:1440,height:1000}});page.on('response',async r=>{if(r.url().includes('api.recent.design')){try{fs.appendFileSync('references/recent-api.jsonl',JSON.stringify({url:r.url(),body:await r.text()})+'\n')}catch{}}});await page.goto('https://recent.design/i/p1lu7hn-family',{waitUntil:'domcontentloaded'});await page.waitForTimeout(6000);await page.screenshot({path:'references/recent-page.png',fullPage:true});console.log(await page.locator('body').innerText());console.log(await page.locator('video').evaluateAll(v=>v.map(x=>({src:x.currentSrc,poster:x.poster,duration:x.duration}))));console.log(await page.locator('a').evaluateAll(v=>v.map(x=>({text:x.textContent,url:x.href}))));await b.close()})();
